# App A（Serpio）詳細調査報告

- 調査対象: <https://serpio.homisoftware.net/>
- 比較対象: Touge Drive Explorer（本リポジトリ）
- 調査日: 2026-09-07（Asia/Tokyo）
- 調査方法: 実ブラウザ操作、DOM/アクセシビリティツリー観察、公開配信ファイルの静的解析、現行リポジトリのコード・テスト・ビルド確認
- 関連計画: [APP_A_UPDATE_ROADMAP.md](./APP_A_UPDATE_ROADMAP.md)

## 0. この文書の読み方

本書では、情報の確度を次の3種類に分ける。

- **観測**: 実際の画面操作、DOM、ネットワーク資産、公開ページ、配信コードで確認できたこと。
- **推定**: 複数の観測から合理的に推定したこと。サーバー内部など未確認部分を断定しない。
- **提案**: Touge Drive Explorer に取り込むべき設計方針。App A の実装を複製する指示ではない。

App A の利用規約は、選定・評価・構成等の独自性を保護対象としている。したがって、本調査の目的は公開挙動から有用な UX パターンを学ぶことであり、非公開データ、正確な評価係数、文言、画像、コードを複製することではない。Touge Drive Explorer では独自の指標、デザイン、データ処理として再実装する。

## 1. 結論

App A の完成度が高く感じられる最大の理由は、個々の機能数ではなく、次の設計が一貫しているからである。

1. **「見つける → 比べる → 理解する → 現地へ行く」が一画面で閉じる。**
2. **検索、並び替え、フィルター、カード、地図、3D が同じ選択状態を共有する。**
3. **複雑な地形・道路データを、順位、色、短いチップ、サムネイルへ圧縮している。**
4. **デスクトップでは固定一覧＋地図、モバイルでは地図＋3段階ボトムシートに役割を分けている。**
5. **高精細表示と軽量表示を利用者が切り替えられ、重い処理は Worker・遅延生成・仮想化・キャッシュで吸収する。**
6. **3D は多機能にしすぎず、視点プリセットと色分けの切替だけで道路特性を読める。**
7. **端末の現在地、外部ナビ、走行済み記録が探索の最後までつながっている。**

Touge Drive Explorer は、投稿、評価、コミュニティ、ルート作成、推薦、天候・交通、複数の3D体験など、総機能では明確に広い。一方で、機能が複数画面・複数モードに分かれ、最初の数十秒で「どの道が自分に合うか」を判断する情報密度と同期感が弱い。最優先は機能追加ではなく、**探索シェルの再設計**である。

推奨する最初の完成形は次である。

- 地図を主役にした探索画面
- すぐ開く横断検索
- 目的別の並び替えプリセット
- コンパクトで比較可能なカード
- カード・地図・検索の双方向同期
- モバイル3スナップシート
- 選択コースだけを段階的に詳しく表示
- 端末性能に応じた軽量/高精細モード

## 2. 調査範囲と制約

### 2.1 実施した動的調査

- 初回画面、地域読み込み、一覧、地図の確認
- 検索オーバーレイの入力、候補選択、該当カードへの移動
- 4種類の並び替えと説明文、地図順位の同期確認
- 距離・料金・交通量・走行状態フィルターの UI 確認
- カード選択、カード内アクション、追加メニューの確認
- 地図レイヤーメニューと「道の駅」表示の切替
- 設定画面の表示モード、時間帯色、距離リング、ナビアプリ選択の確認
- 3D 表示、視点プリセット、色分け、構造物表示の操作
- デスクトップ幅とモバイル幅でのレスポンシブ挙動確認
- 購入導線、ログイン導線、利用規約、プライバシー、データ出典の確認
- DOM 上のラベル、ランドマーク、ボタン寸法、状態属性の確認

### 2.2 意図的に行わなかった操作

- ログイン、アカウント作成、購入、申請、問い合わせ送信
- 「行く」による実ナビ開始
- 走行済み状態の永続変更
- 現在地を第三者の経路サービスへ送信する操作
- 負荷試験、大量取得、認証回避、非公開 API へのアクセス

「行く」の最終挙動は、表示された安全確認 UI と公開配信コードの分岐までを確認した。実行時には現在地からコース入口までの経路を外部サービスで計算し、選択したナビへ渡す構造と読めるが、実データ送信は行っていない。

### 2.3 静的調査の範囲

- HTML、CSS、JavaScript、Web App Manifest、Service Worker
- 公開 JSON のファイル構成とデータ項目
- Worker、地図プロトコル、タイル処理、3D関連資産
- 公開されている API パス、ローカル保存キー、状態遷移
- 現行 Touge Drive Explorer の主要コンポーネント、計算ロジック、スタイル、依存関係、ビルド成果物

配信ファイルの解析は「仕組みを理解するための限定的な観察」であり、ソース全文や独自データの転載はしていない。

## 3. App A の体験設計

### 3.1 初回利用

初回状態は、全国の大量データをいきなり出すのではなく「峠を探しに行こう」という目的を提示し、都道府県または現在地から開始させる。お知らせが先に出る場合があり、その後に位置情報取得を試す。取得できない場合はエラーを短く表示して、都道府県選択へ戻れる。

良い点:

- 空状態が「何もない」ではなく、次の操作を説明している。
- 位置情報が失敗しても主要機能が止まらない。
- 読み込む地域を限定するため、初期データ量と地図混雑を抑えられる。

改善余地:

- 初回の位置情報要求は、利用目的を要求直前に説明した方が安心感が高い。
- お知らせと位置情報エラーが重なると、初見では注意の優先順位が分かりにくい。

### 3.2 地域読み込み

観測時は「神奈川県 +4」のように、選択県と隣接4県をまとめて扱い、全47件を表示した。都道府県ごとに分割されたデータを必要時に取得する構成で、全国一括読み込みを避けている。

検索画面には47都道府県が地域別にまとまり、各県の件数が表示される。「現在地の周辺をまとめて読み込む」も同じ画面にあり、地域選択と名称検索が別導線になっていない。

この設計の価値は、ユーザーが「県名を知らない道」と「近所の道」のどちらからでも同じ探索状態へ入れることである。

### 3.3 検索

検索ピルを開くと、次の構成になる。

- プレースホルダー: 「峠名・都道府県・市町村で検索」
- 地域別の都道府県一覧と件数
- 現在地周辺の一括読み込み
- 入力文字に即応する候補一覧
- 一致箇所の赤色ハイライト
- 候補ごとの県、市町村、有料等の短い補足

「伊豆」と入力すると、西伊豆スカイライン、伊豆スカイライン、西天城高原道路の3件が即時表示された。伊豆スカイラインを選ぶと、検索を閉じ、該当カードを表示領域へスクロールし、地図をその道路へフィットし、選択線を赤いグローで強調した。

ここで重要なのは検索結果ページへ遷移しないことである。検索は「現在の探索ビューで注目対象を決める操作」であり、一覧と地図の文脈を保持する。

### 3.4 並び替え

確認できたプリセットは次の4種類。

| プリセット | UI の説明 | ユーザーの意図 |
|---|---|---|
| バランス順 | コーナーと高低差のバランスが最も良い道 | まず候補を知りたい |
| テクニカル順 | ヘアピンや急コーナーが密集する道 | 操作量の多い道を探したい |
| なめらか順 | ゆるめのコーナーをヒラヒラと流せる道 | 流れのよい道を探したい |
| 標高順 | 上り下り区間が多く標高差の大きい道 | 起伏を重視したい |

変更時には、カードの順位、表示順、地図マーカーの数字、ルート色、凡例が一斉に更新された。たとえばテクニカル順では、三ヶ根山スカイライン、鳳来寺山パークウェイ、三河湾スカイライン等が上位に移動した。

完成度に寄与している点:

- 「スコア」ではなく「何を楽しみたいか」を選ばせる。
- 並び順の説明が選択肢内にあり、用語を推測させない。
- 地図と一覧の順位が一致し、空間とランキングを同時に理解できる。
- 上位20件は数字マーカーで強調され、それ以降は線中心になる。

### 3.5 フィルター

上部の横スクロール可能なチップとして、次を確認した。

- 周辺50km
- 無料区間
- 交通量少
- 未走行
- 走行済

選択中は黒背景の反転表示になり、絞り込みボタンには有効件数が付く。チップ列の端にはフェードマスクがあり、横に続きがあることを暗示する。

良い点は、峠探索で頻繁に使う制約だけを一次 UI に置き、詳細条件のフォームを常時見せないことである。現行アプリの多軸評価は価値が高いが、最初から全項目を見せるより、この「即時判断用5条件」と高度な条件を分離した方が扱いやすい。

### 3.6 一覧カード

デスクトップでは左パネル幅がおよそ390px、カード幅がおよそ361px、高さがおよそ146pxだった。1画面に4〜5件が見え、比較に必要な情報を高密度で載せる。

カードの主な情報:

- 順位バッジ
- 道路名
- 距離
- 最高速度標識風の表示。未登録の場合は説明付き「?」
- 都道府県、市町村
- 有料、交通量等のチップ
- コーナー傾向の色帯
- 標高差
- 上り区間数、下り区間数
- 地形・道路形状が分かる航空写真サムネイル
- 「行く」「路面」「web」「3D」「…」

サムネイルは単なる写真ではない。航空写真タイル、ルート線、標高表現をクライアント側 Canvas で合成し、カード寸法の約2倍で描画している。表示対象に近いカードから生成し、キュー、同時実行数制限、メモリキャッシュを使う構造が見えた。

カードの「…」は画面下シートを開き、選択道路のミニ形状、名称、場所、距離と「走行済」スイッチだけを表示した。低頻度操作をカード本体から追い出している。

DOM 上で全47件と表示されている状況でも、実体化されていた `.card` は約12件だった。スクロール範囲近傍だけを描く仮想化により、画像・Canvas・イベントの負荷を抑えている。

### 3.7 カードと地図の同期

選択のフィードバックは複数層にまたがる。

1. カードがアクティブになる。
2. 必要ならカードが自動スクロールする。
3. 地図カメラが道路全体へフィットする。
4. 選択道路が太く、赤いグロー付きで前面化する。
5. 他の道路は相対的に弱くなる。
6. 順位やフィルター変更ならカードとマーカーを同じ順序で再計算する。

この双方向同期は App A から最も優先して学ぶべき部分である。Touge Drive Explorer にも選択状態はあるが、全機能で一つの `selectedCourseId`、`visibleCourseIds`、`sortPreset`、`filterState` を共有する構造をより明示すべきである。

### 3.8 地図

初期地図は航空写真を基調とした傾斜付き3Dで、道路形状が一目で分かる。観測した主な要素:

- MapLibre GL による WebGL 地図
- 国土地理院の航空写真ラスタ
- 国土地理院 DEM を変換した terrain と hillshade
- 初期 pitch 約55度、bearing 約-10度
- terrain exaggeration 約1.2
- maxPitch 80、world copies 無効
- 高精細時の device pixel ratio 上限約2.5、軽量時約2
- 道路線を順位帯・スコア帯で色分け
- 上位道路の数字マーカー
- 選択線の多重レイヤーによるグロー
- 現在地から10km刻み、最大70kmの測地線距離リング
- ズームに応じたリングのフェード
- 左右回転ボタン

順位帯はおおむね上位10%、30%、60%、残りで分け、件数が少ない場合にも最低境界を持たせる実装と読めた。これは色の差を安定させるための方式で、正確な数値や色は独自に設計すべきである。

### 3.9 地図レイヤー

右上のレイヤーボタンから、次を切り替えられた。

- 道の駅
- 国道・県道
- 居住エリア
- 高速道路
- 都道府県

配信コードには燃料関連レイヤーの痕跡もあるが、通常 UI では表示されなかったため、未公開・条件付き・開発途中の可能性がある。実在機能として扱わない。

「道の駅」を有効にすると地図上へシンボルが追加された。レイヤー選択を独立したポップオーバーにし、通常時の地図を散らかさない点がよい。

### 3.10 地図の時間帯表現

設定には「自動、朝、昼、夕、夜」がある。自動は時刻に応じて変化する。時間帯変更により UI のアクティブ状態だけでなく、地図上の色調・明るさ・大気表現が次の再描画で変化した。

静的資産には `transmittance.bin`、`scattering.bin` 等の LUT があり、カスタム WebGL レイヤーで空・大気・ビネットを表現している。単純な CSS フィルターだけではなく、地図レンダリングの一部として時間帯を扱う構成である。

ただし、探索アプリとしては美観と判読性の競合がある。Touge Drive Explorer で導入する場合、昼を標準にし、夜・夕景は演出モードとする方が安全である。

### 3.11 軽量/高精細モード

設定から次を選べる。

- 軽量: 動作優先
- 高精細: 見た目優先

軽量時は `body` に `lite` が付与され、terrain を無効化し、描画解像度上限を下げる。高度な見た目を一律に諦めるのではなく、利用者が端末に合わせて選べる。

Touge Drive Explorer ではさらに自動判定を加えたい。初回は `deviceMemory`、`hardwareConcurrency`、画面 DPR、WebGL renderer、フレーム時間から推奨モードを決め、設定から上書き可能にする。

### 3.12 3D 表示

カードの「3D」は、地図上の terrain を傾けるだけではなく、全画面の Three.js ビューを開く。

確認できた構成:

- 暗い背景と切り抜かれた地形メッシュ
- コース線と破線の3Dグリッド
- 斜め、真上、側面の視点プリセット
- マウス/タッチで回転・ズームする OrbitControls
- 色分けモードの切替
- 構造物表示の切替
- 高DPI Canvas。1144×646の表示で内部 Canvas は約2288×1292

コーナー色分け:

- 低速コーナー: 赤
- 中速コーナー: オレンジ
- 高速コーナー: 緑
- 直線: 灰

勾配色分け:

- 上り: 赤
- 下り: 青
- 平坦: 灰

側面表示では地形断面と道路の上下動が読みやすく、構造物を有効にすると橋・トンネル等を示すブロック表現が現れた。通常ユーザーには印刷・コピー系の管理 UI は隠されていた。

App A の3Dが優れているのは、見せる指標を一度に一つへ絞ることにある。Touge Drive Explorer の3Dは Overview、Driving Preview、Route Model など機能が豊富なので削る必要はないが、入口を一つに統合し、最初は「コーナー」「勾配」の2択と3視点だけを見せ、詳細操作を二段目へ送るとよい。

### 3.13 モバイル

390×844 相当で確認した構成:

- 地図が全画面の基盤
- 上部に検索ピル
- 直下に横スクロール可能なフィルターチップ
- 右側にレイヤー、購入、現在地、設定等のフローティングボタン
- 下部に一覧ボトムシート
- 初期状態では約56pxのピークだけを表示

CSS/状態ロジック上、ボトムシートにはおおむね次の3状態がある。

- peek: 地図を最大化し、一覧の存在だけ示す
- half: 約44dvh
- full: 約80dvh

半透明背景と約14pxの backdrop blur を使う。`:has(.panel.half/full/card-peek)` により、シート状態に応じて地図コントロールや attribution を移動・非表示にし、重なりを避ける。

ドラッグ操作そのものは自動化ツール上でスナップ変化を再現できなかったが、DOM/CSS/JavaScriptに3状態とポインター処理が存在することは確認した。実機タッチでの追試をリリース前に行うべきである。

### 3.14 ナビゲーション

設定で Google Maps または Yahoo!カーナビを選び、記憶できる。ナビ開始前には次の趣旨の安全確認を求める。

- 道路情報が現況と異なる場合がある
- 標識・規制・現地状況を優先する
- 端末操作は停車して行う

同意バージョンは、ログイン済みならサーバーへ保存する構造が見えた。「行く」は現在地から道路入口までの経路を `/api/route` で取得し、選択した外部ナビ URL/App へ渡す構成である。経路プロバイダーとして LocationIQ、TomTom、Geoapify、openrouteservice が出典に列挙される。

優れている点は、単なる座標リンクではなく「今いる場所から、走り始める側の入口まで」を製品内の責任範囲として扱うこと。ただし、位置情報を第三者へ渡すので、同意、送信先、保持期間、失敗時フォールバックを明示する必要がある。

### 3.15 走行済み・同期

ログインは任意。未ログイン時は走行済み等を localStorage に保持し、ログインすると AWS Japan リージョン側で端末間同期すると説明される。

確認できた保存対象・状態:

- 走行済み、走行日、気になる道路
- 読み込み済み都道府県
- 並び替えプリセット、フィルター
- 現在地、距離リング、地図レイヤー、時間帯
- ナビ開始中の一時状態
- 通知既読、同意バージョン
- 認証、同期キュー、権利情報

ナビから戻った時点を検知し、走行済み判定に結びつける状態もある。正確な自動判定条件は未確認であり、提案では誤判定しにくい「候補提示 → ユーザー確認」を推奨する。

### 3.16 課金・アーリーアクセス

購入 FAB は「表示する峠を増やす」という価値訴求で、早期アクセスの手順を示した。公開規約上の構成:

- Serpio Pass: 買い切り
- 購入時に都道府県スポット枠を2つ付与
- スポット利用: 1都道府県を72時間閲覧
- 自動更新なし
- スポット購入は Pass 保有者向け

配信コードでは Stripe.js v3 と埋め込み Checkout 関連処理、価格、権利、チケット消費、決済確認 API が見える。実購入はしていないため、決済完了 UX、キャンセル、返金、3-D Secure、復元処理は未評価。

## 4. App A の技術構成

### 4.1 フロントエンド

観測した主要技術:

- MapLibre GL のES moduleとshared moduleをセルフホスト
- Three.js 系コードを `vendor-three.js` として分離
- 単一の `app.min.js` と `style.min.css`
- `window.App` を中心とした命令的DOM更新
- 専用 Worker: `pref-worker.js`、`photo-worker.js`、`terrain-worker.js`
- Web App Manifest、Service Worker
- Google Analytics、Microsoft Clarity、Sentry

HTML コメントから、MapLibre の npm パッケージを独自ビルド処理でまとめ、アプリ JS/CSS を縮小して配信する流れが読み取れた。React/Vue等のランタイムは明瞭には見えず、公開 bundle 上は軽量な独自構成に見える。

初期HTMLから確認した概数:

- `style.min.css`: 約157,821 bytes
- `app.min.js`: 約331,649 bytes
- フォント: Noto Sans JP 400/500/700
- PWA: standalone、白背景/テーマ、192/512/maskable アイコン

操作を重ねた後のページ資産インベントリは233件、そのうち画像206件、script 13件、style 2件、その他12件、inline SVG約91件だった。これは初期ペイロードではなく、航空写真・DEMタイルと3D資産を読み込んだ後の総数である。

### 4.2 公開データ分割

ファイル構成から次の戦略が見える。

- `search-index.json`: 軽量な名称・地域検索
- `notices.json`: お知らせ
- `data-version.json`: データ世代
- `targets-filter-stats.json`: フィルター集計
- `targets/{prefecture}/target.slim.json`: 都道府県単位の軽量本体
- 有料/スポット権利用の別インデックス・データ

全国検索に必要な最小インデックスと、表示に必要な都道府県データを分離している。検索候補の表示時点では重い座標列を不要にし、選択地域だけ詳細を読むのが要点である。

### 4.3 データモデル

公開データ・変換処理で確認できた主な概念:

- `geometry_list`: 道路の座標列
- `road_section`: 区間種別、コーナーレベル
- `elevation_smooth`: 平滑化標高列
- `elevation_section`: 上り・下り・平坦区間
- `elevation_unevenness`: 起伏区間
- 建物数、建物フットプリント
- トンネル、橋梁区間
- 有料区間
- 重要物流道路区分
- 速度制限
- 長さ、標高差、建物密度、各種正規化スコア

表示用 ViewModel では、距離、標高差、コーナー種別比率、勾配比率、上り下り数、建物密度、通行属性等をまとめる。名称が同じ道路や県境道路を扱うための安定キーと重複除去もある。

### 4.4 ランキング方式

配信コードから、高水準では次の方式と判断できる。

1. 道路ごとに複数特徴量を抽出・正規化する。
2. プリセットごとに重みを切り替える。
3. 加重合成して総合値を作る。
4. フィルター後の集合を順位付けする。
5. 相対順位帯を色へ変換する。

テクニカル、なめらか、標高では重みの上書きがある。正確な係数は App A 固有の知的成果に当たり得るため本書には転載しない。Touge Drive Explorer は、既存の7軸評価、曲率、標高、道幅、ユーザー評価を基に独自の説明可能なプリセットを作るべきである。

推奨する独自方式:

- 各入力値を外れ値に強い percentile rank または winsorized z-score へ正規化
- 「曲がり」「流れ」「起伏」「景観」「総合」の目的別ベクトルを定義
- 欠損値を0扱いせず、信頼度で合成比率を調整
- スコアと同時に上位寄与要因を2つ返す
- ユーザー評価はベイズ平滑化し、母数の少ない高評価を過大評価しない
- プリセットのバージョンをデータに記録し、順位変動を再現可能にする

### 4.5 地形タイル処理

App A は GSI の DEM PNG をそのまま MapLibre に渡すのではなく、カスタムプロトコルと Worker で MapLibre が解釈できる画像へ変換している。観測した特徴:

- `gsiphoto://` 相当のカスタムプロトコル
- Photo Worker で航空写真タイルの補正・フィルタ処理
- Terrain Worker で DEM PNG を ImageBitmap に変換
- OffscreenCanvas / ImageBitmap の利用
- LRU キャッシュ。観測コードでは変換済み約256、元画像約128を上限とする構成

これによりメインスレッドの画像変換負荷を下げる。Touge Drive Explorer も MapLibre terrain を使用しているが、カードサムネイルや高度解析まで含めて Worker 境界を整理する余地がある。

### 4.6 Service Worker とオフライン

Service Worker はバージョン `v7`。確認できた方針:

- 同一オリジン一般資産: network-first
- フォント、LUT、glyph: cache-first
- GSIタイル: cache-first + 上限付きLRU
- データファイル: 最大12件程度。30MB超はキャッシュしない
- データバージョン更新時: 古いクエリ版を退避・削除
- オフライン時: `ignoreSearch` を使った旧版フォールバック
- API、認証応答: キャッシュ対象外
- 旧キャッシュ: activate 時に整理
- タイル上限: 約2000件。コメント上は約40〜120MBを想定

優れている点は、すべてを precache せず、性質別に戦略を変えること。Touge Drive Explorer の現在の PWA build は約2.29MiB、14エントリを precache している。地図タイルと地域データには別のランタイムポリシーが必要である。

### 4.7 バックエンド境界

公開フロントから確認できた API パス:

- `/api/route`
- `/api/route/attribution`
- `/api/nav-events`
- `/api/early-access`
- `/api/me`
- `/api/me/profile`
- `/api/me/sync`
- `/api/me/consent`
- `/api/me/entitlements`
- `/api/me/prices`
- `/api/me/checkout`
- `/api/me/checkout/confirm`
- `/api/me/tickets/use`
- `/api/me/x-link`
- `/api/me/apply`

認証は AWS Cognito の東京リージョンと Google ログイン、PKCE を使う構成が見える。これらはフロントから見える契約だけであり、バックエンドの言語、データベース、IAM、内部構成は未確認である。

### 4.8 観測性

次の外部サービスが組み込まれている。

- Google Analytics: 利用分析
- Microsoft Clarity: セッション/操作分析
- Sentry: エラー監視

完成度向上には利用観測が有効だが、Touge Drive Explorer で導入する場合は目的別に最小化し、同意、保持期間、IP/位置情報、録画マスキング、削除依頼を明確にする。特に地図・現在地アプリでセッション録画を採用する場合、位置や検索語をマスクする設計が必須である。

## 5. デザインシステムと情報設計

### 5.1 観測した視覚規則

- 基調: 白〜薄灰の面、黒い本文、赤 `#E10600` 系アクセント
- フォント: Noto Sans JP、400/500/700
- 角丸: 約8px中心。カードとピルで用途を分ける
- 境界: 細い灰色線と弱い影
- 選択: 黒反転または赤アクセント
- 地図上: 強い原色を順位・選択に限定
- 余白: 大きな余白より、高密度カード内の整然とした整列を重視
- アイコン: SVG中心、短いテキストラベルと併用

App A の「綺麗さ」は装飾の多さではない。道路形状、順位、勾配など主役だけに彩度を使い、その他を白・黒・灰へ抑えることで、航空写真の情報量と競合しない。

### 5.2 情報の優先順位

カード内の読む順番は次のように設計されている。

1. 順位と名前
2. 距離、速度、場所、有料/交通
3. コーナー傾向、標高差、上り下り数
4. 道路形状サムネイル
5. 現地へ行く・路面を見る・3Dで理解する

ユーザー評価、説明文、投稿などをすべて最初のカードへ入れない。比較に必要な「客観値」と次の操作だけを残している。

Touge Drive Explorer はユーザー評価が強みなので消すのではなく、カードでは総合値と回答数だけに圧縮し、選択後の詳細パネルで7軸・コメント・信頼度を展開すべきである。

### 5.3 モーション

選択、カメラ移動、シート、オーバーレイには短いトランジションがある。OSの `prefers-reduced-motion` に対する処理は一部に見えるが、全アニメーションを包括的に抑える設計までは確認できなかった。

提案:

- カメラ `flyTo` を reduced-motion 時は `jumpTo` にする
- シートはフェード/即時スナップへ切り替える
- 3D自動回転を既定で無効、または明示開始にする
- 200msを超える装飾アニメーションは目的を再評価する

## 6. アクセシビリティ評価

良好だった点:

- 見えるアイコンボタンに明示的な `aria-label` がある
- トグルに `aria-pressed`、開閉に `aria-expanded` がある
- 道路カードが `article` として認識され、キーボードフォーカス可能
- `main`、`header` 等の主要ランドマークがある
- 観測範囲では、名前のない可視インタラクティブ要素はなかった
- forced-colors 向けCSSが一部ある

課題:

- `<meta name="viewport">` に `user-scalable=no` があり、ズームを妨げる
- タップ対象の多くが推奨44×44pxを下回る
- フィルターチップは高さ約32px
- レイヤーボタン約36px、右側FAB約42px
- カードアクション約29px、三点メニュー約26px
- 並び替えの一部は見かけ上18px程度
- モバイルで地図、シート、フローティング操作が密集する
- 全体的な reduced-motion 対応が不十分に見える

したがって「UIが綺麗」をそのまま真似せず、Touge Drive Explorer では視覚密度を維持しながら操作領域だけ44px以上に拡張する。見た目のボタンが小さくても透明ヒット領域を確保できる。

## 7. データ出典と法的注意

App A の出典画面・規約で確認したもの:

| 用途 | 出典 |
|---|---|
| 道路線、名称、仕様 | OpenStreetMap 由来、ODbL |
| 航空写真、DEM、逆ジオコード | 国土地理院 |
| 重要物流道路、交通ラベル | 国土交通省 国土数値情報 |
| 居住エリア | 2020年国勢調査 |
| 建物フットプリント | Overture Maps、OSM/Microsoft/Esri等の寄与 |
| 道の駅 | 国土交通省、座標・Web補完にWikidata/OSM |
| 写真 | Wikimedia Commons、各ファイルのライセンス |
| 入口までの経路 | LocationIQ、TomTom、Geoapify、openrouteservice |
| 市区町村 | HeartRails Geo API |
| 速度制限 | JARTIC 交通規制情報 |

採用前に必ず各提供元の最新規約を確認する。特に次を別々に扱う。

- 画面 attribution の表示要件
- 派生データベースの公開義務
- タイルの大量取得・オフライン保存可否
- 商用利用・再配布・キャッシュ期間
- 個人位置情報の第三者送信

本書は法的助言ではない。

## 8. App A の強み

### 8.1 製品面

- 中心ジョブが「運転したい道を選ぶ」に絞られている
- 起動から候補比較までのステップが短い
- ランキングが目的語で説明される
- 位置、形状、勾配、コーナーが視覚化される
- 外部ナビと路面確認が自然に続く
- 未ログインでも使え、同期は後から追加できる

### 8.2 UI面

- 情報量が多いのに、色とレイヤーの役割が限定されている
- 地図上の選択強調が強く、迷わない
- デスクトップとモバイルで同じレイアウトを縮小していない
- 頻度の低い操作をポップオーバー/シートへ逃がす
- 空状態、読み込み、失敗、選択状態の言葉が短い

### 8.3 技術面

- 地域単位データ分割
- リスト仮想化
- サムネイル遅延生成
- Workerによる画像/DEM処理
- 描画品質切替
- 資産種別ごとのキャッシュ戦略
- MapLibreとThree.jsの役割分離
- エラー監視と利用分析

## 9. App A の弱み・そのまま採用しない点

- 主要なタップ対象が小さい
- `user-scalable=no` は採用しない
- 航空写真＋3Dを常時既定にすると、低性能端末、通信量、文字判読性に不利
- フィルターチップが横に隠れ、全条件を把握しにくい
- 赤・黒の状態表現は強いが、色だけに依存する場面がある
- 現在地の取得・送信タイミングは、より明示的な事前説明が望ましい
- カードのアクション数は依然多く、44pxターゲットと両立しづらい
- 独自ランキングは便利だが、根拠と不確実性が見えにくい
- データの鮮度や欠損理由を個々のカードから判断しづらい
- 3Dは美しいが、高DPI・地形・航空写真の同時利用はGPU/メモリ負荷が高い
- 分析サービスが複数あり、位置系サービスとしてはプライバシー説明をさらに強められる
- 公開購入導線は早期アクセス依存で、一般ユーザーが得られる範囲を即座に理解しにくい

## 10. Touge Drive Explorer の現状

### 10.1 強み

コードとREADMEから確認した現行機能:

- 2D地図、3D地図、3Dルートモデル、走行プレビュー
- 検索、フィルター、並び替え
- 7軸評価、総合評価、ユーザー評価
- ルート作成、経由点編集、推薦
- 認証、投稿、コミュニティ、友達
- PWA、オフライン
- 天候、ライブ交通、通行料金、ユーザー報告
- 独自のベクターベースマップとterrain/hillshade
- モバイルボトムシート

現行 MapView は OpenFreeMap ベクター、Terrarium DEM、terrain、hillshade、検索半径、コース線、選択時等高線・注釈、ルート作成点などを扱う。App A より機能的に高度な箇所が多い。

### 10.2 現行評価ロジック

- 総合値: コーナー、標高、道幅等の主要指標65%、その他35%
- システム値とユーザー値: システムを約5票相当の事前値として混合
- 推薦: 類似度90%、品質10%
- 曲がり推定: 座標角度が約0.18radを超える変化と距離を利用
- 標高: 範囲とタグ等を利用

これらは App A にない説明可能性を作れる資産である。ただし現在はUI上で「なぜこの順位か」へ十分変換されていない。

### 10.3 現行カード

CourseList は全コースを通常の `map` で描画し、県/地域、総合、名称、距離、時間、標高差、ユーザー評価件数、料金、タグ等を表示する。データが増えた時の仮想化と、カード画像/形状の段階生成は未導入。

### 10.4 現行3D

Course3DView は次を持つ。

- overview
- driving preview
- 3D route model
- MapLibre terrain
- SVG/メッシュ表現
- グレード区間、等高線
- 描画順制御

機能を追加するより、入口・初期状態・操作説明・視点プリセット・指標切替を統一する方が効果が高い。

### 10.5 品質ベースライン

`npm run check` の結果:

- ESLint: 成功
- テスト: 26ファイル、128テスト成功
- Production build: 成功
- PWA precache: 14エントリ、約2.29MiB
- CSS: 約147.84kB（gzip約25.83kB）
- main JS: 約484.16kB（gzip約154.15kB）
- Firebase chunk: 約640.26kB（gzip約190.27kB）
- map chunk: 約1,053.01kB（gzip約283.18kB）
- 500kB超 chunk の警告あり

コード品質の基礎は良好だが、初期表示に不要な Firebase・地図・3Dのロード境界をさらに分離できる。

## 11. 比較表

| 項目 | App A | Touge Drive Explorer | 判断 |
|---|---|---|---|
| 機能量 | 探索中心で限定 | 投稿・評価・推薦・交通等まで広い | 現アプリ優位 |
| 初回理解 | 目的と開始点が明確 | 選択肢が多く学習量が大きい | App A優位 |
| 検索 | 県選択と名称検索を統合 | 機能はあるが探索全体との一体感が弱い | App A型へ整理 |
| 地図/一覧同期 | 順位、選択、カメラが強く同期 | 選択同期はあるが機能間の状態が散る | 最優先改善 |
| カード比較 | 高密度、形状サムネイル、順位 | 評価情報は豊富、比較効率は低め | 再構成 |
| モバイル | map-first、3スナップ | シート機能あり、要素数が多い | 現基盤を磨く |
| 3D | 単純で理解しやすい | 多機能で高度 | 入口のみApp A型 |
| ランキング | 目的別4プリセット | 多軸評価・推薦 | 独自統合で差別化 |
| 性能 | 仮想化、Worker、分割、品質切替 | code split/PWAあり、map chunk大 | 改善余地大 |
| アクセシビリティ | ラベル良好、対象サイズに課題 | reduced motion/contrast基盤あり | 現アプリで優位を狙う |
| 社会機能 | 小さい | コミュニティ/友達あり | 現アプリ優位 |
| 運転前後 | 外部ナビ・走行済み連携が短い | 経路作成等が豊富 | 短い導線を追加 |

## 12. 採用すべき設計原則

### 原則A: 探索状態を一元化する

最低限の状態:

```ts
type ExploreState = {
  query: string;
  loadedRegionIds: string[];
  selectedCourseId: string | null;
  hoveredCourseId: string | null;
  sortPreset: 'balanced' | 'technical' | 'flow' | 'elevation' | 'scenery';
  filters: {
    radiusKm: number | null;
    toll: 'all' | 'free';
    traffic: 'all' | 'low';
    visit: 'all' | 'visited' | 'unvisited';
  };
  sheetSnap: 'peek' | 'half' | 'full';
  mapMode: 'auto' | 'lite' | 'quality';
};
```

検索選択、カードクリック、マーカークリックのいずれも `selectedCourseId` を更新し、派生セレクターがカード順、地図GeoJSON、凡例、カメラ命令を作る。各コンポーネントが独自に絞り込み・順位を再計算しない。

### 原則B: 同じ数値を見せるのではなく、意思決定へ翻訳する

悪い例: 「曲率0.82、標高0.67、幅員0.43」だけを見せる。

良い例: 「切り返し多め」「長い上り」「狭め」「評価の根拠: コーナー密度・標高差」。詳細画面で元データと算出方法を確認できるようにする。

### 原則C: 初期表示、選択時、詳細時で情報を段階化する

- 初期: 名前、順位、距離、地域、2つの特徴
- 選択: 形状、標高、主要評価、行く/保存/3D
- 詳細: 7軸、レビュー、天候、交通、料金、投稿、分析根拠

### 原則D: 高度な描画は能力ではなく予算で管理する

画質モードごとに予算を定義する。

| 項目 | Lite | Quality |
|---|---:|---:|
| DPR上限 | 1.5 | 2.5 |
| terrain | off/低解像 | on |
| hillshade | 簡略 | on |
| カード形状 | SVG線のみ | 航空写真合成可 |
| 3D建物 | off | 選択時のみ |
| 影/大気 | off | on |
| 同時サムネイル生成 | 1 | 2〜3 |

### 原則E: 外部送信は操作直前に説明する

現在地、外部ナビ、解析サービス、写真検索は送信先と目的を明示する。設定画面の奥だけでなく、初回実行直前に短い説明を出す。

## 13. 再現テスト手順

今後 App A の変更を追跡する場合の最小手順:

1. 新規プロファイルでトップを開き、初回案内、位置情報前説明、空状態を記録。
2. 任意の県を選び、隣接県、件数、初期カメラ、初期並び順を記録。
3. 検索で道路名、県、市町村、存在しない文字列を試す。
4. 各並び替えで上位5件、マーカー番号、凡例、URL/保存状態を記録。
5. フィルターを単独・複合で試し、件数0の状態を記録。
6. リスト中央・末尾へスクロールし、DOMカード数、画像生成、メモリを記録。
7. カード、マーカー、検索結果から同じ道路を選び、同期挙動を比較。
8. レイヤーを1つずつ有効化し、ズーム閾値、凡例、attributionを確認。
9. Lite/Qualityを同条件で切り替え、terrain、DPR、FPS、転送量を比較。
10. 3Dを各視点・色分け・構造物で確認し、初回生成時間と操作FPSを測定。
11. 390×844、768×1024、1440×900でシート・コントロール衝突を確認。
12. キーボードのみ、200%ズーム、reduced-motion、forced-colorsで確認。
13. オフライン再訪し、シェル、地域データ、地図、エラーメッセージを確認。
14. ナビ、ログイン、購入は専用テストアカウント・テスト位置・決済sandboxで別途実施。

変更追跡では、各操作前後に次を保存するとよい。

- URL、viewport、時刻、ログイン状態、位置権限
- DOM snapshotとアクセシビリティツリー
- computed style の主要トークン
- performance entries の種別別件数/転送量
- localStorage/sessionStorage のキー一覧。値は個人情報を除外
- MapLibre source/layer一覧とcamera state
- Canvas寸法、DPR、Long Task、FPS近似
- スクリーンショット

## 14. 未確認事項

- 実機でのボトムシート慣性・スナップ感
- 低速Android端末のフレームレートとクラッシュ率
- オフラインで実際に利用可能な範囲
- ナビ経路プロバイダーの選択・フェイルオーバー順
- 走行済み自動判定の閾値と誤判定
- ログイン後の同期競合解決
- 決済完了、キャンセル、復元、返金 UX
- 管理者向け3D印刷/コピー機能
- 公開されていない燃料レイヤーの状態
- データ更新頻度と品質保証工程
- 正確なランキング係数

これらは不正な探索をせず、正規のテスト環境・利用許可がある場合だけ追試する。

## 15. 参照した公開ページ・資産

一次情報:

- トップ: <https://serpio.homisoftware.net/>
- できること: <https://serpio.homisoftware.net/about>
- 利用規約: <https://serpio.homisoftware.net/terms>
- プライバシーポリシー: <https://serpio.homisoftware.net/privacy>
- Web App Manifest: <https://serpio.homisoftware.net/manifest.webmanifest>
- Service Worker: <https://serpio.homisoftware.net/sw.js>

配信構造の確認対象:

- `/assets/app.min.js`
- `/assets/style.min.css`
- `/assets/maplibre-gl.mjs`
- `/assets/maplibre-gl-shared.mjs`
- `/assets/vendor-three.js`
- `/pref-worker.js`
- `/photo-worker.js`
- `/terrain-worker.js`
- `/search-index.json`
- `/data-version.json`
- `/targets-filter-stats.json`

## 16. 調査環境の確認

このセッションの必須開発支援MCPについて `codex mcp list` で確認し、次が有効だった。

- Playwright
- Frontend Debugger
- Axe Accessibility
- IcoGenie

実ブラウザ操作には利用可能なコンピューター操作インターフェースを用い、静的解析とアクセシビリティ観察で補完した。

