# レコメンドと作成中ルートの分離：確認手順

## 自動テスト

`npm run check`

- `src/lib/discoveryIsolation.test.ts`: 作成中の地点の有無で探索リクエスト・結果が変わらないこと、明示した通過条件の反映、通信失敗と該当なしの区別、標高未取得、同時リクエスト数。
- `src/lib/draftInsertion.test.ts`: 再評価での地点重複防止、既存の始点・経由地・ゴールを保持した候補挿入、挿入位置指定、採用後の経由地追加。

## ブラウザ確認（保存せず下書きで実施）

1. コース登録を開き、箱根ターンパイクを1回だけ組み込む。
   - 12地点となる。24地点に重複しない。STARTとGOALは1つずつ。
2. 「峠道を探す」で韮山反射炉を検索する。
   - 探索中心のピンが表示されるが、ルートの地点数は増えない。
   - 探索中心をタップすると、通過必須ではない旨を表示する。
3. 詳細の通過条件は未指定、半径25km、最大距離10kmで探索する。
   - 箱根の12地点を必須条件にせず、韮山周辺を探索する。
   - 通信不能なら取得失敗を表示し、「コースがない」と断定しない。
4. 候補をルートに追加する。
   - 元のSTART/経由地/GOALが保持され、候補はGOALの前に1回だけ追加される。
5. 明示的にレコメンドの始点・必須経由地を設定し、詳細条件を折り畳む。
   - 適用中の通過条件が見え、個別または一括解除できる。
   - 解除しても作成中ルートの地点は削除されない。
6. 探索場所を変更して、検索中に別の場所へ入力し直す。
   - 以前の探索中心で検索できない。遅れて返った古い検索結果で上書きされない。

外部サービスの可用性、道路・標高データ、条件によって候補数は変わるため、常に候補が存在することは合格条件にしない。

## 現在地からの周辺探索：追加確認

自動テストでは位置情報・道路応答を模擬し、実際の位置情報や本番保存データは使用しない。

- `currentLocation.test.ts`: 一時的な取得失敗の再試行、権限拒否時に再試行しないこと、時間切れの説明、不正座標の拒否、低精度の明示、探索中心と通過条件の分離。
- `externalDiscovery.test.ts`: 最大距離2・4・6・10・40kmで、周辺の探索地点を半径内に生成すること。
- `discoveryIsolation.test.ts`: 中心付近に候補がない場合の周辺探索、明示した始点・ゴール・経由地を勝手に外さないこと、全通信失敗時に追加リクエストを増やさないこと。

実機での確認手順（GPS・権限設定の実挙動は端末ごとに確認）：

1. 先に始点・経由地・ゴールを作成し、峠道探索の「現在地を使う」を押す。
   - 取得中表示が出る。以前の探索中心・候補は破棄され、取得中は候補検索できない。
   - 取得成功後は「現在地」が探索エリアとなる。作成中ルートの地点は増減しない。
2. 明示的な通過条件なし、半径25km、最大距離4kmまたは6kmで検索する。
   - 現在地を通る必要はなく、中心付近で候補がなければ半径内の別の場所も調べる。
   - 距離上限や峠の適格条件を緩めて候補を水増ししない。
3. 一度地名で探索エリアを設定してから、位置情報を拒否した状態で「現在地を使う」を押す。
   - 許可設定の確認を案内する。以前の探索エリアで検索が続行されない。
4. 現在地取得中に地名を入力し直す。
   - 遅れて届いた位置情報が、新しい地名の検索結果を上書きしない。
5. 実機でGPS取得が時間切れになった場合は再試行を待つ。
   - 再試行後も失敗したら、時間切れの説明と地名検索の代替手段が表示される。
