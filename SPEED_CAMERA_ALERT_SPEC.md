# 速度注意・取締情報レイヤー 仕様検討書

- 作成日: 2026-09-16
- 対象: 峠 — Touge Drive Explorer
- 状態: 実装前の推奨仕様
- 目的: 固定式・半固定式・可搬式の速度取締情報を地図へ分かりやすく表示し、利用者が明示的に有効化した場合だけ、安全運転を促す視覚・音声警告を行う

## 1. 結論

この機能はPWAでも実装できる。ただし、対応範囲を次のように分ける必要がある。

| 利用状態 | PWAでの実現性 | 判断 |
|---|---:|---|
| アプリを前面表示し、画面が点灯している | 高い | 初期版として採用可能 |
| 他アプリを一時的に開く | 端末・OS依存 | 警告継続を保証しない |
| 画面消灯・ロック中 | 低い | PWAでは保証不可 |
| アプリを完全に閉じた状態 | 不可 | ネイティブ機能が必要 |
| 地図上のアイコン・範囲表示 | 高い | PWAで実装可能 |
| 前面表示中の音声・効果音 | 高い | ユーザー操作後に有効化 |
| バックグラウンドGPS常時監視 | 低い | ネイティブ/ハイブリッド版が必要 |

PWAのService Workerは、任意のタイミングで常駐し続ける仕組みではない。アイドル時に終了し、位置情報APIもService Worker上では利用できない。Web Pushはサーバーから届いたイベントを通知する仕組みであり、端末の現在地をバックグラウンドで監視して近接判定する代替にはならない。

したがって初期版は、利用者が明示的に開始する「ドライブモード」を前提とする。画面消灯中も確実に動かしたくなった段階で、Capacitor等によるiOS/Androidパッケージを検討する。

## 2. 機能名と位置づけ

「オービス回避」ではなく、次のような安全運転支援として設計する。

- 機能名: **速度注意・取締情報**
- 警告文: 「この先、速度に注意してください」
- 目的: 規制速度の遵守、生活道路・事故多発路線での注意喚起
- 表示は参考情報であり、取締りの有無や現在位置を保証しない
- 情報がない場所でも取締りが行われる可能性があることを明記

警視庁の公開情報でも、重点場所以外で交通実態に応じたランダムな取締りを行うとされている。可搬式は移動するため、「ここに必ず設置されている」という固定点として扱ってはいけない。

## 3. 対象データの分類

### 3.1 固定式

道路脇、道路上、トンネル等に恒常設置される地点。

- 地図表現: カメラ型の実線アイコン
- 色: 赤橙
- 警告: 進行方向と道路が一致した場合のみ
- 主な初期データ候補: OpenStreetMap `highway=speed_camera`、`enforcement` relation

OpenStreetMap上の点は利用者編集データであり、完全性・最新性は保証されない。データの最終確認日、出典、方向情報の有無を保存する。

### 3.2 半固定式

複数の設置拠点間で撮影装置を移動運用できるもの。警察庁資料では高速道路での運用例が示されている。

- 地図表現: カメラ型アイコン＋点線リング
- 色: 黄橙
- ラベル: 「半固定式候補地点」
- 稼働中と断定しない

### 3.3 可搬式・移動式

三脚等で一時設置されるもの。正確なリアルタイム位置を恒久データとして持つことはできない。

次の2層に分ける。

1. **公式公開の重点路線・実施場所**  
   路線または区域として表示。公開月、時間帯、警察署、出典URLを持つ。
2. **利用者報告**  
   将来検討。位置、報告時刻、方向、確認数を持ち、数時間で自動失効させる。法務、モデレーション、虚偽報告対策を整えるまでは導入しない。

公式資料が路線や行政区までしか示さない場合、勝手に1点へ変換せず、線または半透明の範囲として描画する。

## 4. 地図UI

### 4.1 レイヤー

設定画面の「MAP LAYERS」に次を追加する。

- 固定式カメラ
- 半固定式候補地点
- 公式の速度取締重点路線
- 可搬式の利用者報告（将来、既定オフ）

既存の「公式コース／自分のコース／フレンドのコース」とは別グループにし、混同させない。

### 4.2 アイコン

| 種類 | アイコン | 色 | 地図上の意味 |
|---|---|---|---|
| 固定式 | カメラ | 赤橙 | 登録された固定地点 |
| 半固定式 | カメラ＋点線リング | 黄橙 | 装置が設置され得る拠点 |
| 重点路線 | 盾＋破線 | 青紫 | 警察が公開した重点路線・区域 |
| 可搬式報告 | 三脚＋時計 | 黄 | 期限付き利用者報告 |
| 情報不確実 | `?` バッジ | 灰 | 方向・状態等が未確認 |

密集時はクラスタリングし、ズームに応じて個別表示する。運転中はアイコンを押さなくても意味が分かるよう、色だけでなく形も変える。

### 4.3 詳細カード

停止中にアイコンを選択すると、次を表示する。

- 種類
- 方向
- 対象道路・路線
- 規制速度（信頼できる場合だけ）
- 出典
- 最終確認日または有効期間
- 情報の確度
- 「情報が古い」「撤去済み」「位置が違う」の報告

走行中は詳細カードを開かず、警告だけに限定する。

## 5. 警告設定

初期状態はすべてオフ。利用者が停止中に明示的に有効化する。

### 5.1 通知方法

- オフ
- 画面のみ
- 音声のみ
- 画面＋音声
- 効果音のみ
- バイブレーション（対応端末だけ）

Vibration APIは対応状況が限定的なので、補助機能としてfeature detectionする。音声はWeb Speech API、効果音はWeb Audioまたは事前読込した短い音源を使う。

### 5.2 対象

- 固定式
- 半固定式候補地点
- 公式重点路線
- 可搬式報告（将来）

### 5.3 距離

固定距離ではなく、走行速度から到達時間を基準にする。

推奨初期値:

```text
一次警告距離 = clamp(現在速度 × 25秒, 300m, 1200m)
二次警告距離 = clamp(現在速度 × 10秒, 150m, 500m)
```

GPS速度が不明な場合は600mを使う。利用者設定は「早め／標準／短め」の3段階にし、走行中に細かい数値を操作させない。

## 6. 近接判定アルゴリズム

単純な半径判定だけでは、並行道路、反対車線、立体交差で誤警告が多くなる。次の順で判定する。

1. GPS精度が許容範囲内か確認する。
2. 現在地から候補地点/区間までの距離を計算する。
3. GPS headingまたは直近数点から進行方位を求める。
4. カメラの対象方向がある場合、方位差を確認する。
5. 現在の道路または選択ルートと同一コリドーか簡易map matchingする。
6. 到達時間が閾値内に入ったら一次警告。
7. さらに接近した場合だけ二次警告。
8. 通過後は同じ対象を一定時間再通知しない。

推奨フィルター:

- `accuracy > 50m` の位置は警告判定に使わない、または確度を下げる
- 低速・停止中は音声警告を抑制
- 方位差45〜60度超は対象外
- 位置が閾値の境界を往復しても再通知しないようhysteresisを入れる
- トンネル等でGPSが飛んだ場合に突然警告しない
- 固定点は空間インデックスで近傍だけ検索する

## 7. ドライブモード

利用者が「ドライブ開始」を押したときだけ動作する。

開始時に次を行う。

1. 位置情報の利用目的を説明
2. geolocation権限を要求
3. 警告音を試聴し、音声出力をユーザー操作で有効化
4. 対応端末ではScreen Wake Lockを取得
5. `watchPosition()` を開始
6. 近隣データだけをIndexedDBへキャッシュ
7. 画面上に「ドライブモード中」「GPS精度」「音声状態」を常時表示

終了時は位置監視、Wake Lock、音声キューを停止する。タブが非表示になった、Wake Lockが解除された、位置権限が失われた場合は、警告が保証されないことを明確に表示する。

## 8. 警告UI

画面を注視させないことを最優先にする。

### 一次警告

- 画面上部に大きな帯
- カメラ/盾アイコン
- 「この先、速度に注意」
- おおよその距離
- 2〜3秒で縮小
- 音声: 「この先、速度に注意してください」

### 二次警告

- 短い効果音または「まもなくです」
- 地図の対象アイコンをパルス表示
- 全画面モーダルや操作要求は出さない

### 禁止するUI

- 走行中の確認ボタン
- 閉じる操作を要求する警告
- 小さい文字だけの説明
- 突然の長文読み上げ
- 未確認情報を「現在取締中」と断定

警察庁は、停止中を除くスマートフォン等の保持使用や画像注視を禁止・注意喚起している。設定変更、詳細閲覧、報告投稿は停止中だけ許可する設計にする。

## 9. データモデル

```ts
type EnforcementType = 'fixed' | 'semi-fixed' | 'official-zone' | 'community-mobile'
type EnforcementGeometry =
  | { type: 'Point'; coordinates: [number, number] }
  | { type: 'LineString'; coordinates: [number, number][] }
  | { type: 'Polygon'; coordinates: [number, number][][] }

interface SpeedEnforcementFeature {
  id: string
  type: EnforcementType
  geometry: EnforcementGeometry
  roadName?: string
  directionDegrees?: number
  maxSpeedKph?: number
  operator?: string
  sourceName: string
  sourceUrl: string
  sourceLicense: string
  confidence: 'official' | 'verified' | 'community' | 'unknown'
  validFrom?: string
  validUntil?: string
  lastVerifiedAt: string
  expiresAt?: string
}

interface SpeedAlertPreferences {
  enabled: boolean
  fixed: boolean
  semiFixed: boolean
  officialZones: boolean
  communityMobile: boolean
  output: 'visual' | 'voice' | 'both' | 'sound' | 'off'
  timing: 'early' | 'standard' | 'late'
  volume: number
}
```

## 10. データ取得案

### 固定式

- OpenStreetMapの `highway=speed_camera` と `enforcement` relationを定期取得
- アプリ起動時にOverpassへ直接投げ続けず、サーバー側で地域別データを生成・キャッシュ
- ODbLの帰属表示と、派生データベースに該当する場合のライセンス対応を行う
- OSMに存在しないことを「カメラなし」と解釈しない

### 公式重点路線・可搬式運用情報

- 都道府県警・警察署の公開取締情報を出典URL付きで正規化
- PDF/HTMLのフォーマットは地域ごとに異なるため、アダプター方式にする
- 自動解析後は人手確認を経て公開
- `validFrom` / `validUntil` を必須にし、期限切れを自動非表示
- 公開情報が行政区・路線までなら、その粒度を維持する

### 利用者報告

初期版には入れない。導入条件:

- 虚偽・嫌がらせ報告の通報とモデレーション
- 同一端末の連投制限
- 数時間での自動失効
- 正確な位置情報を一般公開することの法務確認
- 投稿者の位置履歴を不要に保存しない設計
- 「利用者報告」「未確認」の明示

## 11. プライバシー

- 近接判定は原則として端末内で行う
- GPS履歴は既定でサーバーへ送信しない
- ドライブ終了時に一時位置バッファを破棄
- 走行ログ保存は別の明示オプトイン
- 利用者報告を送る場合も、報告地点だけを送り、そこまでの軌跡を送らない
- Firebase Analytics等へ正確な緯度経度を送らない

## 12. 法務・安全上の留意点

- 日本向け初期版でも、配布前に速度取締情報表示の法務確認を行う
- 海外展開時は国ごとに警告装置・データ表示の規制が異なるため、地域別feature flagが必要
- 出典の利用条件と再配布条件を確認
- 警察公開資料のURLを示し、公式提供アプリであるかのように見せない
- 取締り情報の有無に関係なく規制速度遵守が必要であることを表示
- 運転中の画面操作を誘発しない

## 13. 推奨ロードマップ

### Phase 1: PWA前面動作MVP

- 固定式OSMデータ
- 地図アイコンと表示トグル
- ドライブモード
- 前面表示中のGPS近接判定
- 画面・音声・両方・オフ
- Wake Lock
- 方向フィルター、二段階警告、再通知抑止
- ローカル設定保存

### Phase 2: 公式重点路線

- 対象地域を1都県に限定して公開資料アダプターを作る
- 路線・区域・有効期間表示
- 更新ジョブと期限切れ処理
- 出典、確度、最終更新の詳細カード

### Phase 3: 品質向上

- ルートとのmap matching
- トンネル・並行道路・反対車線の誤警告削減
- オフライン地域パック
- GPS精度、電池消費、誤警告率の実走評価
- Android/iOS実機テスト

### Phase 4: ネイティブ/ハイブリッド版

- 画面消灯・バックグラウンド位置監視
- Android foreground service
- iOS background location modeの適用可否審査
- OSネイティブ音声・通知
- PWAと同じReact UI・判定エンジンを再利用

### Phase 5: 利用者報告（採用判断後）

- 期限付き報告
- 信頼度と複数人確認
- 通報・モデレーション
- 法務・ストア審査確認

## 14. 受け入れ条件

- 設定初期値は警告オフ
- 固定式・半固定式・重点路線の見た目が区別できる
- 反対方向・並行道路での誤警告をテストする
- 同じ対象を短時間に繰り返し警告しない
- 位置権限拒否時に機能全体が壊れない
- 音声非対応でも画面警告へフォールバックする
- タブ非表示・画面ロック時に「警告継続を保証しない」と明示する
- 古い可搬式情報を自動非表示にする
- 出典・最終確認日・確度を確認できる
- GPS履歴を既定でサーバーへ送信しない
- 運転中に操作を求めない

## 15. 参考資料

- 警察庁「交通取締り資機材等の整備について」: https://www.npa.go.jp/policies/budget/review/r6/siryou_R6_2.pdf
- 警視庁「公開交通取締り」: https://www.keishicho.metro.tokyo.lg.jp/kotsu/torishimari/kokai_juten/kokai.html
- 警察庁「やめよう！運転中のスマートフォン・携帯電話等使用」: https://www.npa.go.jp/bureau/traffic/keitai/info.html
- OpenStreetMap `highway=speed_camera`: https://wiki.openstreetmap.org/wiki/JA:Tag:highway%3Dspeed_camera
- OpenStreetMap License FAQ: https://osmfoundation.org/wiki/Licence_and_Legal_FAQ
- Geolocation API: https://developer.mozilla.org/en-US/docs/Web/API/Geolocation_API
- ServiceWorkerGlobalScope: https://developer.mozilla.org/en-US/docs/Web/API/ServiceWorkerGlobalScope
- Screen Wake Lock API: https://developer.mozilla.org/en-US/docs/Web/API/Screen_Wake_Lock_API
- SpeechSynthesis: https://developer.mozilla.org/en-US/docs/Web/API/SpeechSynthesis
- Vibration API: https://developer.mozilla.org/en-US/docs/Web/API/Vibration_API
