# コースナビゲーション：実装方針

更新日: 2026-09-16

## 今回実装した土台

- コース詳細から開くGPS自動計測で、高精度位置情報を取得する。
- GPSの `coords.speed` をkm/hへ変換し、地図上に速度HUDを表示する。
- コースの始点120m以内で計測開始、始点から離れた後にゴール120m以内で計測停止する。
- これは走行記録補助であり、経路案内そのものではない。

## 段階的なナビゲーション仕様

### Phase 1: コース追従ガイド

- 選択コースの線からの距離、最寄りの進捗点、残距離を端末内で算出する。
- コース外へ一定距離・一定時間外れた場合は「コースに戻ってください」を一度だけ音声通知する。
- 表示は速度、次のランドマーク、残距離だけに抑える。

完了条件: 位置誤差を考慮して誤警告を抑え、GPSの精度が悪いときには案内を停止して精度不足を表示する。

### Phase 2: ターンバイターン案内

- 出発前にコースの経由点を道路ネットワークにスナップし、交差点単位の案内ステップを生成する。
- 各ステップは `距離・方角・道路名・案内文・座標` を持つ。
- 走行中は次の一手だけを音声中心で案内する。速度計と安全警告を最優先にする。

完了条件: ルーティング提供者の商用利用条件、利用上限、障害時フォールバックを満たす。公開OSRM等の共有エンドポイントを本番依存にしない。

### Phase 3: 車載向け体験

- 画面ロック・バックグラウンド・CarPlay/Android Autoを要件に含める場合は、PWA単体ではなくネイティブの位置情報バックグラウンド実行を検討する。
- 地図の操作は停車時のみとし、運転中の表示密度を下げる。

## 制約

PWAの測位と音声は、画面が前面にありOSが実行を維持している範囲で動作します。バックグラウンドや画面ロック中の連続ナビは端末・OSによって保証できません。安全に関わる主機能として出すにはネイティブ化または対応OSを明記します。
