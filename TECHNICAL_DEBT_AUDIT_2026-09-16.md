# 峠アプリ 横断手直し監査レポート

- 監査日: 2026-09-16
- 対象: `main` / `041c708`
- 主対象: 直近の地図表示設定、パーソナライズ、ダークモード、モバイルボトムシート、共有・フレンド、PWA、および既存機能との接続部分
- 目的: 機能追加と連続修正で生じた不整合、隠れた不具合、セキュリティ・運用・拡張性上の負債を、実装に着手できる粒度で整理する

> 実施状況（2026-09-16）: P0の旧フレンド閲覧、位置・再生情報の失効、MapLibre GL JS更新はコードへ反映済みです。Firestoreルールの実機エミュレータ試験と本番デプロイは未実施です。P1の削除500件制限、コメント順序、失敗を0件として扱う表示、シート挙動、テーマの不整合のうち、削除分割・順序・失敗表示・シート挙動・主要ダークテーマは反映済みです。残タスクは本書の完了条件を満たしたときにのみ完了とします。

## 1. 結論

基本品質は壊れていない。`lint`、138件のユニットテスト、TypeScriptビルドはすべて成功した。一方、現在の最大リスクは見た目ではなく、共有権限と公開中データの寿命にある。

最優先で直すべきものは次の3点。

1. コースの閲覧ルールが、現在のフレンド関係・ブロック状態を確認していない。過去の `allowedViewerIds` に残った利用者は、フレンド解除後やブロック後もコースを読める可能性がある。
2. 直接依存する MapLibre GL JS に、npm監査で critical のXSS脆弱性が1件検出された。
3. 位置情報・再生曲の共有は、権限取消、測位エラー、再生終了時に古い公開情報を消さない経路がある。

UI面では、直近のダークモード修正を「個別セレクタへの追記」で積み重ねているため、新画面追加のたびに同種の漏れが再発しやすい。また、ボトムシートは純粋関数テストこそあるが、実DOM上の複数シート、タップ、ドラッグ、キーボード、テーマ切替を検証するE2Eテストがない。

## 2. 調査方法と確認結果

### 2.1 実施した確認

- 直近8コミットと変更量を確認
- `App.tsx`、共有設定、地図ルート表示、パーソナライズ、ボトムシート、MapLibre、Firebaseアクセス層、Firestoreルール、PWA登録処理を横断して静的監査
- ローカルアプリを実際に開き、モバイル幅のアクセシビリティツリーと現在のルートビルダー状態を確認
- `npm run check` を実行
- 本番依存のみを対象に `npm audit --omit=dev` を実行
- バンドルサイズ、テスト種別、外部API接続箇所、色指定、巨大コンポーネントを集計

### 2.2 自動チェック結果

| 項目 | 結果 |
|---|---|
| ESLint | 成功 |
| Vitest | 138件すべて成功 |
| TypeScript / Vite build | 成功 |
| npm production dependency audit | critical 1件 |
| ReactコンポーネントE2E | 未整備 |
| Firestoreルールテスト | 未整備 |
| 視覚回帰テスト | 未整備 |

ビルドは成功するが、Viteの500 kB警告を超えるチャンクが3つある。

| チャンク | おおよそのサイズ |
|---|---:|
| MapLibre | 1,053 kB |
| Firebase | 639 kB |
| アプリ本体 | 506 kB |

PWA precacheも約2.34 MiBになっており、モバイル初回起動・更新時の待ち時間に影響する。

## 3. 優先度定義

- **P0**: 公開前または直ちに対応。権限、個人情報、critical脆弱性
- **P1**: 次の機能追加より先に対応。データ不整合、主要UX、規模拡大で顕在化する障害
- **P2**: 近いスプリントで対応。再発防止、性能、アクセシビリティ、保守性
- **P3**: 継続改善。長期的な整理や軽微な整合

## 4. P0 — 直ちに直すべき事項

### P0-1. フレンド解除・ブロック後もコース閲覧権限が残り得る

#### 根拠

`firestore.rules` の `canViewCourse()` は、種別、管理者、所有者に加え、現在のUIDが `allowedViewerIds` に含まれるかだけを確認している。

現在のフレンド関係を確認する `friendWith(courseData.authorId)`、`blockedViewerIds`、`globalBlockedViewerIds`、プロフィールの `blockedUserIds` は判定に使われていない。

コース作成・編集時には「フレンド全員」またはリストから `allowedViewerIds` をスナップショットとして書き込む。しかし、次のイベントで既存コースを再計算する処理がない。

- フレンド解除
- フレンドリストからメンバーを削除
- 新しいフレンドを追加
- ユーザーをブロック
- リスト自体を削除

`courseAudiences/{courseId}` にはモードとリストIDが保存されるが、Firestoreの読み取り認可には使われていない。

#### 影響

- 元フレンドが共有リンクや既知のcourse IDを使い続けられる可能性
- UI上でブロックしたつもりでも、バックエンド権限が止まらない
- 「フレンド全員」が動的な意味ではなく、保存時点のフレンド全員になる
- 新しいフレンドは既存コースを見られない一方、外したフレンドは見続けるという非対称が起きる

#### 推奨修正

短期対応として `canViewCourse()` の限定共有分岐に、少なくとも以下を加える。

1. 現在もコース所有者と `friendWith(authorId)` である
2. `blockedViewerIds` と `globalBlockedViewerIds` に閲覧者UIDが含まれない
3. リストサイズと型をルールで検証する

同時に、フレンド関係またはフレンドリスト変更時に、Cloud Functionsまたは信頼できるサーバー処理で該当コースの `allowedViewerIds` を再計算する。クライアントだけに同期責任を置くと、オフライン、アプリ強制終了、旧バージョン利用で漏れる。

ブロック機能がまだ未完成なら、型だけ残して安全機能があるように見せず、P0対応と同時に仕様を確定する。

#### 完了条件

- フレンド解除直後に旧共有コースの `get` が permission denied になるルールテスト
- ブロック直後に permission denied になるルールテスト
- 新規フレンド、リスト追加・削除、リスト削除時の再計算テスト
- 管理者、所有者、公式コースの例外が維持されること

---

### P0-2. MapLibre GL JS のcritical XSS脆弱性

#### 根拠

`npm audit --omit=dev` で、直接依存 `maplibre-gl@5.24.0` に critical 1件が検出された。

- Advisory: `GHSA-jrc7-96c5-q579`
- 内容: `DOM.sanitize()` の迂回によるXSS
- 影響範囲: `maplibre-gl <= 6.4.0`
- npmが提示する修正版: `6.10.0`

現在のアプリはポップアップ生成に `setDOMContent()` と `setText()` を使い、アプリコードから未加工の `setHTML()` は呼んでいないため、直接の攻撃面は抑えられている。ただし、依存ライブラリ内の脆弱性を抱えた状態に変わりはなく、地図はアプリの中心機能なので放置しない。

#### 推奨修正

- MapLibre 6.10系以上へ更新
- 主要APIの破壊的変更を確認
- 通常地図、3D地図、マーカー、ポップアップ、ナビゲーション、現在地、ダーク/ライトスタイルを回帰確認
- 更新後に `npm audit --omit=dev` を再実行
- CSP導入または強化も別タスクで実施

---

### P0-3. 位置共有が測位失敗・権限取消後も残り得る

#### 根拠

`App.tsx` の `watchPosition()` エラーコールバックは何もしない。位置共有スイッチを明示的にオフにした場合は presence を削除するが、次の状況では古い座標がFirestoreに残る。

- OS設定から位置情報権限を取り消す
- 測位が継続的に失敗する
- アプリを異常終了する
- 長時間起動しない
- ネットワーク切断中にオフ操作が失敗する

presenceドキュメントには `updatedAt` があるものの、購読側は鮮度を判定せずそのまま表示する。サーバー側TTLや定期削除も見当たらない。

#### 推奨修正

- 測位エラー時に共有を停止し、presenceの `location` を `null` にするかドキュメントを削除
- UIに「共有中 / 許可待ち / 測位失敗 / 最終更新」を表示
- クライアント表示で、一定時間より古い位置を描画しない
- Firestore TTLまたはサーバー側の期限切れ削除を導入
- `presence` ルールで座標形式、経緯度範囲、許可対象数、`updatedAt == request.time` を検証
- 失敗時の削除書き込みも失敗した場合に再試行キューへ積む

## 5. P1 — 次の大きな機能追加より先に直す事項

### P1-1. 再生終了後も古い曲名が共有される

Media Sessionのポーリングは、曲情報が見つかったときだけ保存する。metadataが消えた場合は何もせず、最後に共有した `nowPlaying` とpresenceが残る。

対策は、直前に曲が存在していた状態からmetadataなしへ遷移したとき、一度だけ `nowPlaying: null` を保存し、位置共有もない場合はpresenceを削除すること。更新時刻を表示し、古い曲をUIで自動的に非表示にする防御も必要。

### P1-2. 地図の個別表示設定とフレンド絞り込みの優先順位が説明と一致しない

UIは「個別の設定は上の初期表示より優先」と説明している。しかし実装は、フレンドのスコープ判定を個別オーバーライドより先に行う。

その結果、選択中リスト外のフレンドコースは一覧から消え、個別に「表示」を指定できない。既に `show` が保存されていても、フレンドスコープで先にfalseになる。

次のどちらかへ仕様を統一する。

- 個別設定を最優先にする: 明示的な `show/hide` → フレンド絞り込み → 種別デフォルト
- フレンド範囲を上限にする: UI文言を「選択したフレンドの中で個別調整」に変更し、範囲外コースと無効化理由も一覧に表示

設定項目の意味からは、後者の方が誤公開を避けやすい。一方、これは地図への表示設定であって閲覧認可ではないため、利便性重視なら前者も選べる。仕様決定後、優先順位を純粋関数1か所へ集約する。

### P1-3. 「接続回復後に同期する」という表示に対応する同期機構がない

プロフィール保存、外観、検索プリセットなどの失敗メッセージには「接続回復後に再試行」「同期します」とある。しかし、outbox、IndexedDBキュー、`online` イベントでの再送、保留中表示は実装されていない。

ローカルキャッシュへ残る項目はあるが、自動同期ではない。これは利用者に誤ったデータ保証を伝える。

短期は文言を「端末には保存しました。再接続後に保存操作を行ってください」へ正す。中期は、設定パッチ単位の永続outbox、直列送信、指数バックオフ、競合解決、同期状態表示を実装する。

### P1-4. Firestore入力検証に穴がある

#### 評価

評価型は7軸だが、ルールで1〜5を検証しているのは `curves`、`elevation`、`width`、`scenery` の4軸だけ。`surface`、`traffic`、`access` は未検証で、巨大値や不正型により集計を汚染できる。

#### コメント

コメント更新は、更新前の `authorId` が本人であれば全フィールドを変更できる。`authorId`、`authorName`、`createdAt`、`likeCount` まで差し替えられる。

#### コース

コース作成・更新は、名前とrouteの最低条件以外のスキーマ検証が薄い。座標範囲、配列上限、文字数、`allowedViewerIds` の上限と関係性、評価オブジェクト、URL等をルールで制約していない。

ルールで `keys().hasOnly()`、変更可能フィールド、文字数、配列長、数値範囲、サーバー時刻、作成者不変を明示する。クライアントの型とバリデーションはセキュリティ境界ではない。

### P1-5. コース読み込みがN+1で、件数制限とページングがない

`loadPublicCourses()` は公式、自分、共有の3クエリを行った後、各コースのratingsサブコレクションを1件ずつ読む。コース100件なら、初期表示だけで最大103クエリ相当になる。

さらに各一覧クエリに `limit`、カーソル、ページングがなく、管理画面も全件取得する。

対策:

- 評価集計をコース文書へ非正規化し、トランザクションまたはCloud Functionsで更新
- 一覧は軽量サマリーをページング
- 詳細を開いたときだけコメント・個人評価などを読む
- 管理画面もカーソル方式にする
- 読み取り回数と費用を計測できるログを追加

### P1-6. 人気コースの削除がFirestoreの500操作上限を超える

`deleteCourse()` は評価、いいね、コメント、ライブ情報、オーディエンス、コース本体を1つのbatchへ入れる。関連文書が合計約495件を超えると失敗する。

サーバー側recursive delete、または安全な分割バッチへ移行する。途中失敗を考慮し、削除中フラグ→サブコレクション削除→本体削除の再開可能なジョブにするのが望ましい。

### P1-7. コース一覧取得エラーが黙って「データなし」になる

公式、自分、共有の3クエリは個別に `.catch(() => null)` される。インデックス不足、権限拒否、ネットワーク障害でも、画面は一部または全部のコースが存在しないように見える。

カテゴリ別の成功・失敗を保持し、最低でも「共有コースのみ同期失敗」のように通知する。評価の読み込み失敗で全コース読み込みが落ちないよう、サマリーと詳細の障害境界も分ける。

### P1-8. ボトムシートのタップ契約がコンポーネントごとに違う

共通フックは、最小化→中間→全画面という隣接スナップを意図している。しかし `CommunityPanel` などは `dragProps` を展開した後に独自 `onClick={expandOnTap}` を上書きしている。

これにより、同じ見た目のヘッダーでも次の差が出る。

- 一覧シート: 最小化中のタップで中間へ
- 一部モーダル: ヘッダーのタップで全画面へ
- ドラッグ後の合成click抑止フラグを、独自ハンドラが消費・解除しない

過去に発生した「一度ドラッグすると後のタップが効かない」問題と同系統の再発余地がある。クリック処理を上書きせず、フックへ `tapBehavior` または統一 `handleTopTap` を持たせる。

### P1-9. ダークモード対応が上書きCSSの継ぎ足しになっている

`styles.css` は1,358行、色リテラル参照は約463箇所ある。直近修正では末尾へ100行超のダーク専用セレクタを追加している。

この方式では、詳細度競合、DOM構造変更、新規コンポーネント、hover/disabled/focus/empty/error状態で漏れが再発する。

推奨は、色を次の意味トークンへ移すこと。

- `--surface-app`, `--surface-sheet`, `--surface-raised`, `--surface-input`
- `--text-primary`, `--text-muted`, `--text-disabled`
- `--border-default`, `--border-strong`
- `--accent`, `--accent-contrast`, `--danger`, `--warning`
- `--map-overlay-surface`, `--scrim`, `--focus-ring`

各コンポーネントはテーマを知らず、意味トークンだけを使う。ライト、ダーク、systemを同じUIシナリオでスクリーンショット比較する。

### P1-10. UI回帰とFirestore認可を守るテスト層がない

現在の138テストは主に純粋関数で、質は良い。しかし、最近問題になった箇所を直接守るテストがない。

不足している重要シナリオ:

- 複数ボトムシートが全て最小化されたときだけ段積み表示
- 1枚が中間/全画面なら他シートが背面へ隠れる
- ハンドルタップ、余白タップ、ドラッグ、pointer cancel、iOS Touch Events
- ダーク/ライト/systemで全モーダルと全状態
- 地図スタイル切替後もroute/source/layerが維持される
- 設定保存、閉じる、再度開く、オフライン失敗
- フレンド解除・ブロック・リスト変更後のFirestore認可

Playwright系E2EとFirebase Emulator Suiteのrules-unit-testingを導入する。

### P1-11. 外部API依存がブラウザー直結で、障害・利用制限・プライバシーの境界が曖昧

ブラウザーからOSRM、国土地理院標高、Nominatim、東京大学ジオコーダ、GSI住所検索、Open-Meteo、AWS terrainへ直接通信する。検索語、座標、経路点が第三者へ送られる経路が複数ある。

問題:

- サービスごとの利用規約、レート制限、CORS変更にUIが直接影響
- 同一リクエストのキャッシュとレート制御が統一されていない
- どのデータがどこへ送られるか利用者へ説明しにくい
- 公開OSRM等の可用性がコース作成の可用性になる

検索・ルーティング・標高を薄いサーバーゲートウェイへ寄せ、タイムアウト、キャッシュ、レート制御、監視、プロバイダー切替、データ最小化を統一する。少なくともプライバシーポリシーと画面説明に送信先を明記する。

## 6. P2 — 近いスプリントで直す事項

### P2-1. コメント順序が未指定

コメント購読は `orderBy` なしのcollection snapshotで、表示順は保証されない。`createdAt` の昇順または降順を仕様化し、同時刻の第2キーも決める。

### P2-2. いいね数のために全いいね文書を購読する

件数と自分の状態のためにlikesサブコレクション全体をダウンロードしている。人気コースほど高コストになる。集約カウントと `likes/{uid}` の個別購読へ分ける。

### P2-3. 地図表示オーバーライドが孤児化する

削除済み、共有解除済み、閲覧不能になったコースIDが `mapRouteOverrides` / `hiddenRouteIds` に残る。ユーザー文書が増え続けるため、保存時に現在管理可能なIDで掃除し、件数上限も設ける。

### P2-4. 設定の保存タイミングが統一されていない

テーマは選択時に即時保存されるが、ルート表示、パーソナライズ、プロフィール公開範囲は下部ボタンを押すまでローカルstateだけである。閉じると未保存変更を失うが、dirty表示・確認・sticky saveがない。

次のどちらかに統一する。

- 全項目を即時保存し、保存中/保存済み/端末のみ/失敗を表示
- 明示保存に統一し、sticky footer、未保存バッジ、閉じる確認を用意

モバイル設定では前者の方が自然だが、outbox実装とセットにする。

### P2-5. PWA更新確認のintervalが解放されない

`InstallPrompt` の `onRegisteredSW` は1時間ごとの `setInterval` を作るがIDを保持せず、cleanupもしない。StrictMode、HMR、再マウントで重複する可能性がある。effect/refで1本だけ保持し、unmount時に解除する。

### P2-6. モーダルのフォーカス管理とキーボード操作が不足

`role="dialog"` と `aria-modal` はあるが、フォーカストラップ、初期フォーカス、Escape、閉じた後のフォーカス復元が共通化されていない。

3Dモデル内のランドマークはSVG `g` に `role="button"` と `tabIndex=0` を付けているが、Enter/Spaceの `onKeyDown` がないためキーボードでは起動できない。

共通Dialog/BottomSheet基盤へフォーカス管理を集約し、axeと実キーボードで確認する。

### P2-7. fire-and-forget保存の失敗が利用者へ届かない

位置共有、曲名共有、presence更新など複数箇所で `void save...` を使い、catchがない。UIはオンになっていてもサーバーへ保存できていない状態が起こる。

楽観更新する場合も、失敗時にエラー状態、再試行、必要ならロールバックを行う。共通mutation runnerを作り、処理中の二重操作も抑止する。

### P2-8. 初期バンドルが大きい

MapLibre自体に加え、3Dビュー、管理画面、コミュニティ、コース作成などが静的importされ、アプリ本体も500 kBを超える。

候補:

- `Course3DView`、`AdminPanel`、`CommunityPanel`、レポートフォームを `React.lazy`
- 地図2Dと3Dで重複する地形処理を整理
- Firebase SDKの利用モジュールを再確認
- bundle visualizerをCI成果物に追加
- 起動、地図表示、設定表示のLCP/INPをモバイルで計測

### P2-9. Appと主要コンポーネントが肥大化している

主な行数:

- `App.tsx`: 824行
- `Course3DView.tsx`: 680行
- `MapView.tsx`: 651行
- `externalDiscovery.ts`: 629行
- `CourseForm.tsx`: 535行
- `firebase.ts`: 423行
- `styles.css`: 1,358行

状態、非同期処理、画面遷移、Firebase書き込みが `App.tsx` に集中し、修正の影響範囲が広い。

機能単位に次を分離する。

- `useCourseRepository`
- `usePresenceSharing`
- `useProfileSettings`
- `useRouteBuilder`
- `useSheetCoordinator`
- `courseRepository`, `profileRepository`, `presenceRepository`
- semantic theme tokens / component styles

大規模な一括書き換えではなく、新しいテストを置いてから1機能ずつ抽出する。

### P2-10. ユーザープロフィール全体保存は将来の競合源になる

`saveProfile()` は、画面がロード時に取得したprofile全体をmerge保存する。別タブや別端末で一部設定を変更していた場合、古い値で上書きする可能性がある。

変更フィールドのpatchだけを保存し、`updatedAt` またはrevisionで競合を検知する。特にフレンドリスト、共有範囲、パーソナライズは別設定文書へ分ける選択肢もある。

## 7. P3 — 継続改善

- 公式・自分・フレンドの件数表示について、読み込み失敗と0件を区別する
- コメント・いいね・presenceにskeleton/loading/empty/errorの共通状態を導入
- ルート表示設定の検索条件とタブを、設定画面を閉じても一時保持するか仕様化
- `mapRouteVisibility`、`hiddenRouteIds` など旧フィールドの移行完了後に削除
- `blockedViewerIds`、`globalBlockedViewerIds`、`blockedUserIds` の重複概念を1つの仕様へ整理
- PWA manifestの背景色をテーマと整合させる。起動直後のライト色フラッシュも確認
- 外部SNS iframeの許可、表示失敗、追跡・Cookie説明を整理
- noticeの2.5秒固定消去を、重要度と読み上げ時間に応じて変更
- 地図の表示設定、選択中コース強制表示、検索結果プレビューの凡例を明確にする

## 8. 推奨改修計画

### Phase 0: セキュリティと公開情報の停止保証（1〜2日）

1. MapLibreを安全な版へ更新し、地図回帰確認
2. `canViewCourse()` に現在のフレンド関係とブロック判定を追加
3. Firestore Emulatorで共有認可テストを追加
4. presenceの鮮度判定、測位失敗時クリア、曲停止時クリア
5. 共有・ブロック仕様を1枚の権限表にする

### Phase 1: データ整合と保存UX（2〜4日）

1. 設定保存方式を即時保存または明示保存へ統一
2. 実体のある再試行outbox、または正しい失敗文言
3. フレンド/リスト変更時のコースオーディエンス再計算
4. 地図個別表示とフレンド絞り込みの優先順位を確定
5. 保存失敗を共通mutation runnerで通知・再試行

### Phase 2: 回帰防止（3〜5日）

1. Playwrightでモバイルシート状態遷移テスト
2. ライト/ダーク/systemの視覚回帰
3. axeによる主要画面のアクセシビリティ監査
4. Dialog/BottomSheetのフォーカスとキーボード操作を共通化
5. 直近のユーザー指摘を受け入れ条件としてE2Eへ固定

### Phase 3: データアクセスのスケール対応（3〜6日）

1. 評価といいねの集約値を非正規化
2. コース・管理画面をページング
3. 削除を再開可能なサーバージョブへ移動
4. コメント順序、購読粒度、読み込みエラー表示を修正
5. 外部APIゲートウェイと監視を整備

### Phase 4: UI基盤と性能（継続）

1. semantic color token化
2. 大型画面のlazy load
3. `App.tsx` からpresence、設定、コースrepositoryを抽出
4. bundle/LCP/INP予算をCIへ導入
5. レガシー設定フィールドを移行・削除

## 9. 実装チケットへ切り出す順序

| 順位 | チケット | 完了の目安 |
|---:|---|---|
| 1 | MapLibre critical脆弱性修正 | audit 0 critical、2D/3D回帰成功 |
| 2 | コース共有ルール修正 | 解除・ブロック後の読取拒否をemulatorで証明 |
| 3 | presence期限切れと停止処理 | 古い位置・曲が自動で非表示/削除 |
| 4 | Firestoreスキーマ検証強化 | 評価7軸、コメント差分、コース上限をrules test |
| 5 | ボトムシート共通タップ契約 | 全シート同じ3段階遷移をE2E化 |
| 6 | ダークモードtoken化 | 主要画面のvisual regressionが両テーマで成功 |
| 7 | 設定保存/outbox | オフライン→再接続で自動同期を確認 |
| 8 | コース読取・集計最適化 | 一覧のN+1解消、ページング導入 |
| 9 | 削除ジョブ化 | 500件超の関連データでも完了・再開可能 |
| 10 | コード分割・遅延ロード | 初期app chunkを500 kB未満へ |

## 10. 触らずに維持すべき良い点

- 共有プロフィールは「全ユーザー」を廃止し、フレンド/リスト/非表示へ絞っている
- presence読み取りは現在のフレンド関係も確認しており、コースルールより安全
- コース作成・編集時に一般公開を強制的に無効化している
- 地図テーマはUIテーマと連動する設計になっている
- 地図ルート設定は、種別、検索、タブ、一括操作、個別例外まで揃っている
- ボトムシートの幾何計算は純粋関数化され、ユニットテストがある
- ルーティング、標高、地図スタイル、パーソナライズ等のドメインロジックには既にテスト基盤がある

この土台を残しつつ、次の段階では「機能を増やす」より「権限・公開停止・状態遷移・回帰テストを一貫させる」ことが、完成度を最も大きく上げる。
