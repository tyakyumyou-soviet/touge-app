# ETC利用履歴連携方式 調査報告

調査日: 2026-09-16（Asia/Tokyo）  
対象候補: StackWorks LLC「ETC利用明細 - 経費精算・証明書発行」  
対象候補を選んだ理由: 「ETC利用照会サービスのID・パスワードでログインし、明細・割引・車両番号を自動取得する」という相談内容と公開機能が最も一致したため。

> 注意: ユーザーが念頭に置いているアプリ名は未確定である。別アプリの場合でも、公式サイト側の通信方式、リスク、推奨設計は共通するが、対象アプリ固有の結論は再調査が必要。

## 1. 結論

当初の仮説は大筋で正しい。ただし、現在確認できた資料では次のように修正する必要がある。

| 仮説 | 調査結果 | 確度 |
|---|---|---:|
| 1. ID・パスワードをサーバーへ送信 | Appleの公開メタデータ上は「当社の取得用サーバーを経由」と明記されている。一方、開発者のプライバシーポリシーは「端末から公式サービスへアクセス」と読め、資料が矛盾する。 | 高（サーバー経由の自己申告が存在）／現行構成は未確定 |
| 2. アプリ側サーバーから公式サイトへログイン | Appleメタデータの記述どおりならその構成。公式サイトは通常のHTMLフォーム＋Cookieセッションなので技術的にも成立する。 | 高（少なくとも掲載上の方式） |
| 3. HTMLやCSVを解析 | 公式の公開API/OAuthは確認できず、ログイン後のHTMLまたはCSV/PDF出力を取得・解析する方式が最も整合的。 | 高い推定 |
| 4. 独自DBへ保存 | 開発者ポリシーは「端末内SQLite」と明記。クラウド上の独自明細DBではないと説明している。 | 高（自己申告） |
| 5. 定期的に再ログイン | iOS版更新情報は「アプリを開いたとき」に取得すると説明。サーバーが閉じた状態でも常時巡回する方式ではなく、起動時／手動更新が中心と考えるのが自然。 | 高（起動時同期）／バックグラウンド同期は未確認 |
| 端末内ブラウザーを自動操作 | Android権限にはアクセシビリティサービス等が見当たらず、画面座標操作よりHTTPクライアントでフォーム送信する方式が合理的。WebView利用自体は否定できない。 | 中程度の推定 |

最もあり得る全体像は、次のとおりである。

```text
アプリ起動
  ↓
Keychain / Secure StorageからID・パスワードを読む
  ↓
取得用サーバー経由、または端末から直接、公式ログイン画面をGET
  ↓
JSESSIONID Cookieと画面固有のhidden値 p を保持
  ↓
risLoginId / risPassword / p を公式サイトへPOST
  ↓
ログイン後HTML、または公式CSV/PDF出力を取得
  ↓
HTML/CSVを正規化 → 端末内SQLiteへupsert
  ↓
月次集計・検索・タグ・PDF/CSV出力をアプリUIで提供
```

これは公式API連携ではなく、公式Web画面をプログラムから利用する「非公式Web連携」である可能性が極めて高い。

## 2. 公式サービス側で確認できた事実

### 2.1 公開APIやOAuthではなく、HTMLフォームとサーバーセッション

認証せずに公式ログイン画面の公開HTMLとJavaScriptを確認した。

- ログイン画面: `https://www2.etc-meisai.jp/etc/R?funccode=1033000000&nextfunc=1032000000`
- サーバーは `JSESSIONID` Cookieを発行する。
- Cookieには `HttpOnly` が指定されている。
- レスポンスは `Content-Type: text/html; charset=x-euc-jp`。
- フォーム項目名は `risLoginId` と `risPassword`。
- フォーム内に、リクエストごとに変わると考えられるhidden項目 `p` がある。
- 公開JavaScript `submit.js` はフォームを `POST` にし、`/etc/R?funccode=1013000000&nextfunc=1013000000` へ送信する。

したがって、クライアントは次の一連の状態を保持しなければならない。

1. 初回GETでCookieを受け取る。
2. HTMLを解析してhidden値 `p` を取得する。
3. 同じCookieを付けてID・パスワード・`p` をPOSTする。
4. ログイン後も同一Cookie jarを維持する。
5. 画面遷移用の `funccode` / `nextfunc` とhidden項目を追跡する。

これはOAuth認可コードやAPIトークンを使う構成ではない。調査範囲では、第三者アプリ向けAPI、OAuth、OpenID Connect、開発者ポータル、API利用規約は見つからなかった。ただし「存在しない」と断定するものではなく、「公開情報から発見できなかった」という意味である。

### 2.2 公式に取得できる情報

公式サービスは、ETCクレジットカード／パーソナルカードについて過去15か月の明細を表示し、PDF・CSVを出力できる。最大1,000枚のカードを1ユーザーIDに登録できる。

確認できる主要項目は次のとおり。

- 利用年月日・時刻
- 入口IC・出口IC
- 車種
- 車両番号
- 割引前料金
- ETC割引額／割引表示
- 実際の通行料金
- ETCマイレージ還元額
- 後日確定・料金調整の状態
- 利用証明書の取扱番号

したがって、相談中の「前月比」「割引総額」「結局いくらかかったか」「ナンバー」は、元データが取れればアプリ側で計算・表示できる。ただし、公式データには道路の完全な走行軌跡や緯度経度列はないため、地図上の区間表示には別途ICマスタと経路推定が必要になる。

### 2.3 非公式アプリに対する公式警告

公式運営6社は2025-12-19に、ユーザー情報を入力して利用実績を表示する非公式アプリが存在すると告知し、公式提供ではないこと、損害への責任を負わないこと、公式サイトを利用することを明記した。

候補アプリの初版公開日は2025-11-10で、警告は約1か月後である。ただし、警告文はアプリ名を挙げていないため、この候補アプリを名指ししたものとは断定できない。

公式サイトは現在、新規登録・ログイン・利用実績確認に一部アクセス制限を実施している。理由は明示されていないため、非公式アプリ対策と断定はできないが、自動取得の安定性に直接影響する。

### 2.4 利用規程上の位置づけ

- サービスの目的は、カード発行を受けた本人が明細表示または利用証明書発行を受けることに限定される。
- 規程違反、不適当な行為、その他管理者が妥当と判断した場合、登録取消しの対象になり得る。
- 自動取得やスクレイピングを名指しで禁止する文言は、今回確認した規程にはなかった。
- しかし公式は非公式アプリに明確な注意喚起を出しており、第三者アプリ連携を承認しているとは解釈できない。

したがって「明示的にスクレイピング禁止と書いていない」ことを「商用の認証代理が許可されている」と読むべきではない。実装前に運営事務局への書面照会が必要である。

## 3. 候補アプリ側で確認できた事実

### 3.1 ストア上の機能

- 明細の自動取得
- 月ごとの合計、回数、平均料金、割引合計
- 入口IC、出口IC、割引前料金、割引額、割引種別、割引率
- タグ付けと絞り込み
- CSV、明細PDF、利用証明書PDF
- 複数アカウント
- iOS版は月額200円／年額2,000円
- Android版は1,000件以上のダウンロード

### 3.2 認証情報と明細の保存

Appleの公開メタデータAPIが返す現行バージョン1.9の説明には、以下が明記されている。

- 入力した認証情報を「当社の取得用サーバーを経由」して公式サービスへ送る。
- 通信はHTTPS。
- 認証情報は端末のKeychainへ暗号化保存し、取得時のみ使う。
- 取得した明細は端末内に保存する。

一方、開発者サイトのプライバシーポリシーには以下が記載されている。

- 明細・カード・タグは端末内SQLite。
- 認証情報はSecure Storage。
- 公式サービス `www2.etc-meisai.jp` へのアクセスを除き外部送信しない。
- 明細データは第三者と共有しない。

さらに、一部のApp Store Webページでは「外部サーバーにデータを送信しません」と表示される。このため、公開説明は相互に整合していない。

### 3.3 公開資料の矛盾の評価

可能性は少なくとも3つある。

1. **取得用サーバー方式が現行で、プライバシーポリシーが不正確**  
   Appleの公開メタデータAPIと第三者の保存済みストア情報がこの説明を保持している。
2. **以前は取得用サーバー方式で、端末直結へ移行した**  
   iOS 1.9の公開日と開発者サイトHTMLのHTTP `Last-Modified` がともに2026-08-27であり、同時期に説明を更新した可能性がある。ただし、1.9のリリースノートは方式変更を明記していない。
3. **「外部サーバーへ送らない」が明細保存のみを指し、認証中継サーバーを例外扱いしている**  
   文言の定義が揃っていないだけという可能性もある。

現時点ではパケットキャプチャ、アプリバイナリ、開発者回答のいずれもないため、3つを確定的に選べない。セキュリティ判断では、最も保守的に「認証情報が事業者サーバーを通る可能性あり」と扱うべきである。

### 3.4 自動取得のタイミング

iOS 1.9のリリースノートは「アプリを開いたときの明細取得」「初回起動時も自動で取り込み」と説明している。このため、少なくとも説明上の主同期トリガーはアプリ起動である。

仮に認証情報がKeychainにしか永続保存されないなら、事業者サーバーがアプリ終了中に定期再ログインすることはできない。サーバー側定期同期を行うには、次のいずれかが別途必要になる。

- 事業者サーバーにも認証情報を保存する。
- 長寿命の公式セッションCookieを保存する。
- 端末がバックグラウンドで認証情報を読み、同期要求を起動する。

公開資料はこれらを説明していない。よって「サーバーが常時巡回」は確認できず、起動時同期とみるのが妥当である。

## 4. 実装方式の技術的復元

### 4.1 ログイン処理

概念的な処理は次のようになる。

```pseudo
cookieJar = new CookieJar()

loginPage = GET officialLoginUrl using cookieJar
p = parseHiddenInput(loginPage.html, name="p")

response = POST officialLoginAction using cookieJar with form:
  risLoginId = userId
  risPassword = password
  p = p

if response indicates login failure:
  stop; never retry repeatedly
else:
  continue with same cookieJar
```

注意点:

- 公式HTMLはEUC-JPなので、UTF-8前提で読むと日本語・項目名解析が壊れる。
- `p` を固定値として扱えない。
- `JSESSIONID` とHTMLフォーム状態をセットで保持する必要がある。
- 誤パスワードの反復はアカウントロックにつながる。
- アクセス制限、メンテナンス、画面変更を正常な「明細0件」と誤認してはいけない。

### 4.2 明細取得

考えられる方法は2つある。

**A. HTML表の解析**

- 長所: 画面に表示される状態、リンク、選択情報を直接取得できる。
- 短所: DOM構造、ラベル、ページング変更に弱い。EUC-JP変換も必要。

**B. 公式CSVをセッション内で出力し、CSVを解析**

- 長所: 表形式として安定しやすく、件数の多い明細に向く。
- 短所: CSV発行前の選択／検索フォーム操作が必要な可能性がある。CSV仕様変更には追従が必要。

実用実装では、HTMLで状態・検索期間・出力操作を進め、明細本体はCSVを正規化するハイブリッドが最も堅牢である。ただし候補アプリが実際にこの構成である証拠はない。

### 4.3 重複排除と「未確定→確定」更新

候補アプリはバージョン1.6、1.7で重複取得を修正し、1.7では「未確定から確定へ切り替わる際に同じ明細が二重登録される問題」を明記している。レビューにも重複・金額倍増の報告がある。

これは次の典型的な不具合と整合する。

```text
未確定行: 日時 + 入口 + 出口 + 暫定金額 + 状態=未確定
確定行:   日時 + 入口 + 出口 + 確定金額 + 状態=確定
```

金額や状態を一意キーに含めると、同じ走行が別レコードになる。設計時は可能なら公式の不変な取扱番号を主キーにし、取扱番号が未確定時にない場合は以下を組み合わせた暫定キーを用いる。

- アカウントID
- ETCカード識別子
- 利用日時
- 入口IC
- 出口IC
- 車両番号／車種

確定レコード到着時に暫定行をマージし、料金・割引・状態を更新する。同期単位ごとにupsertし、同一取得バッチ内の重複も除去する必要がある。

### 4.4 PDF・利用証明書

候補アプリは「公式フォーマット」と説明している。最も自然なのは、同じログインセッションで公式PDF発行操作を行い、そのレスポンスを端末へ保存する方式である。アプリ独自に公式風PDFを再生成すると、証明書の真正性や取扱番号確認との整合が難しくなる。

ただし、実際に公式PDFを透過ダウンロードしているか、独自PDFを作っているかは公開情報だけでは確定できない。

## 5. セキュリティ評価

### 5.1 「HTTPS」と「Keychain」の意味

これらは必要だが、取得用サーバー方式の根本リスクを消さない。

- HTTPSは通信経路上の第三者から保護する。
- Keychain / Secure Storageは端末保存時の窃取リスクを下げる。
- しかし中継サーバーが公式へログインするなら、中継サーバーのアプリケーション処理は平文のID・パスワードを一時的に扱える。
- 「保存しない」と「見ることができない」は別である。
- ログ、APM、例外追跡、リバースプロキシ設定に機密値が残らない保証も必要である。

公開情報では、次の事項を確認できなかった。

- 取得用サーバーの運営リージョン／委託先
- 認証情報をログへ残さない実装
- メモリ上の保持時間
- 公式セッションCookieの保存期間
- 第三者セキュリティ監査・SOC 2・ISMS
- TLS証明書ピンニング
- インシデント通知方針
- 脆弱性報告窓口
- 認証情報中継についての公式運営者からの許可

### 5.2 プライバシー表示の不整合

- Apple側では、デバイスIDと利用状況データが広告・トラッキングに使われる可能性が表示される。
- Google Play側では、位置情報、アプリのアクティビティ等の収集・共有、送信中暗号化、データ削除不可が表示される。
- 開発者ポリシーは「位置情報を収集しない」「第三者共有しない」「設定から全データ削除可能」と説明する。

広告SDKによるデータ処理とETC明細本体の処理を分けている可能性はあるが、利用者から見た説明は整合していない。ストアのデータセーフティ情報は開発者申告であり、Appleも未検証と明記している。

### 5.3 運用リスク

- 公式画面変更で同期停止する。
- アクセス制限・レート制限の対象になり得る。
- 誤った自動リトライでアカウントがロックされ得る。
- 未確定料金の更新で重複・誤集計し得る。
- 公式障害とアプリ障害の切り分けが難しい。
- 公式の反映遅延を即時取得できない。
- ユーザーは非公式アプリにID・パスワードを預けるリスクを理解しにくい。

## 6. touge-appへの採用判断

### 6.1 推奨順位

1. **公式CSVのユーザー主導インポート** — 今すぐ実装可能で最も安全。  
2. **公式サイトをアプリ内／外部ブラウザーで開き、ユーザーがCSVを保存して取り込む導線** — パスワードをアプリが受け取らない。  
3. **公式運営6社へAPI・業務連携・自動取得の可否を照会** — 正式連携が可能なら最優先。  
4. **端末内HTTPクライアントによる直接取得** — 書面許可、法務確認、セキュリティ設計後に限定検討。  
5. **自社サーバーへのID・パスワード中継** — 現時点では不採用を推奨。

### 6.2 なぜCSVインポートから始めるべきか

- 公式が正式にCSV出力を提供している。
- アプリがID・パスワードへ触れない。
- 公式の非公式アプリ警告と衝突しにくい。
- サーバー費用が不要で、無料運用できる。
- 地図化、月次比較、割引分析という利用価値の大部分は実現できる。
- 将来公式APIが得られても、正規化後の内部データモデルを再利用できる。

### 6.3 地図表示の追加設計

ETC明細だけでは実走行ルートを一意に復元できない。入口ICと出口ICが同じでも経路候補が複数あるため、表示は次の3段階に分ける。

1. **確定情報**: 入口IC・出口ICをポイント表示。
2. **推定情報**: 高速道路ネットワーク上の最安／最短／料金整合ルートを半透明破線で表示。
3. **実走行補正**: アプリのGPS走行ログが同日時にある場合だけ、実ログと明細を照合して実線表示。

UIには必ず「ETC明細」「推定ルート」「GPS一致済み」を区別する凡例を出し、推定を実績として見せない。

### 6.4 推奨データモデル

```text
etc_import_batch
  id, source_type, imported_at, source_file_hash, status, warning_count

etc_account
  id, display_name, provider_type

etc_card
  id, account_id, masked_card_number, nickname, color

etc_trip
  id, account_id, card_id, official_reference_no?
  used_at, entry_ic_raw, exit_ic_raw, road_name_raw
  vehicle_number_masked, vehicle_class
  price_before_discount, discount_amount, paid_amount
  mileage_redemption, settlement_status
  provisional_key, source_row_hash, first_seen_at, last_seen_at

etc_trip_revision
  trip_id, observed_at, previous_values, new_values

ic_master
  canonical_id, operator, name, aliases, longitude, latitude

etc_route_estimate
  trip_id, geometry, method, confidence, toll_match_delta
```

金額は浮動小数点ではなく整数の円で保存する。カード番号・車両番号は原文のまま表示せず、既定でマスクする。

## 7. 実装ロードマップ案

### Phase 0: 事業・法務確認

- ETC利用照会サービス事務局へ、CSV取込、端末内直接取得、サーバー代理取得の可否を個別に照会。
- 非公式であることの表示要件を確認。
- 認証情報を扱う構成は、回答が得られるまで実装しない。

### Phase 1: 安全なローカル版

- 公式CSVインポート
- 重複排除、未確定→確定マージ
- 月次合計、前月比、割引前／割引額／支払額
- カード・車両番号別フィルター
- 端末内保存、全削除、エクスポート
- インポート元ファイルのハッシュと監査ログ

### Phase 2: 地図連携

- IC名称正規化・別名辞書
- ICポイント表示
- 表示中の明細だけ地図レイヤーを有効化
- 推定区間を破線表示
- GPSログがある場合の照合

### Phase 3: 取得UX改善

- 公式サイトを安全に開く導線
- CSV共有シート／ファイル選択からワンタップ取込
- 差分インポートと更新通知
- 失敗時に「公式未反映」「CSV形式変更」「重複候補」を区別

### Phase 4: 正式連携が得られた場合のみ

- OAuth/APIまたは正式仕様による同期
- 最小権限・失効可能なトークン
- バックオフ、レート制限、監査ログ
- セキュリティレビューと侵入テスト

## 8. 追加で確定するために必要な調査

候補アプリの現行通信経路を確定するには、次のいずれかが必要。

1. 開発者へ「取得用サーバー経由と端末直結のどちらが現行か」を問い合わせる。
2. テスト専用ETC照会アカウントを用意し、利用者の明示同意のもと、端末のDNS／TLS接続先だけを観測する。
3. Android APKまたはiOSアプリの正規取得バイナリを、実行せず静的解析してAPIホスト名を確認する。
4. 取得用サーバーのプライバシー・保存期間・委託先・削除手順の文書を取得する。

実ユーザーのID・パスワードを調査目的で入力してはならない。TLS復号を伴う調査は、テストアカウント、テスト端末、明示的な権限、機密データの保存禁止を前提とする。

## 9. 情報源

### 公式

- [ETC利用照会サービス 非公式アプリへの注意喚起](https://www.etc-meisai.jp/caution/20251219.html)
- [ETC利用照会サービス 利用規程](https://www.etc-meisai.jp/rules.html)
- [公式Q&A: サービス概要・15か月・PDF/CSV](https://www.etc-meisai.jp/faq/12.html)
- [公式ログイン画面](https://www2.etc-meisai.jp/etc/R?funccode=1033000000&nextfunc=1032000000)
- [アクセス制限の実施について](https://www.etc-meisai.jp/info/251127.html)
- [公式サイトポリシー](https://www.etc-meisai.jp/sitepolicy.html)

### 候補アプリ

- [Apple App Store](https://apps.apple.com/jp/app/id6754386167)
- [Apple Search API メタデータ](https://itunes.apple.com/lookup?id=6754386167&country=jp)
- [Google Play](https://play.google.com/store/apps/details?id=com.stackworksdev.etcmeisai&hl=ja)
- [開発者プライバシーポリシー](https://etc-meisai.stack-works.dev/privacy)
- [Chrome-Statsに保存されたストア説明](https://chrome-stats.com/d/id6754386167?hl=ja)

## 10. 調査上の限界

- 候補アプリへ実アカウントを入力していない。
- アプリのTLS通信を傍受していない。
- 正規バイナリを展開・逆コンパイルしていない。
- ログイン後の公式画面は、認証情報なしでは動的確認していない。
- 開発者へ直接質問していない。
- ストアのプライバシー情報と開発者ポリシーはいずれも自己申告を含み、第三者監査結果ではない。

従って、「取得用サーバーを経由するとの現行Appleメタデータ」と「端末直結と読める開発者ポリシー」の矛盾は未解消である。この未解消点自体が、同方式をそのまま採用しない重要な理由になる。

## 11. 追補: PWAなら公式サイト連携が可能になるか

追補日: 2026-09-16

### 11.1 結論

PWAにすると、ETC機能のうち次の部分はかなり実用的に作れる。

- ホーム画面からアプリとして起動
- 公式CSVのファイル選択・取込
- IndexedDBによる端末内保存
- オフラインでの履歴閲覧・集計
- 前月比、割引額、実支払額、車両番号別の分析
- ICポイントと推定区間の地図表示
- PWA更新とキャッシュ

一方、PWAとしてインストールしても、通常のWebページに課されるセキュリティ境界は変わらない。したがって、`touge-app` のJavaScriptからETC利用照会サービスへログインし、ログイン後HTMLやCSVを読み取ることは、公式サイトが明示的にCORS/API連携を許可しない限りできない。

PWAは「Webサイトにアプリアイコン、オフライン、Service Worker等を付けたもの」であり、任意サイトのCookieやHTMLを読めるネイティブ特権を得る仕組みではない。

### 11.2 PWAでも越えられない制約

#### Same-Origin Policy / CORS

`touge-app` と `www2.etc-meisai.jp` は別オリジンである。クロスオリジンのHTMLを `fetch()` して本文を読むには、ETCサイト側が当アプリのオリジンをCORSで許可する必要がある。`mode: no-cors` にしてもレスポンスはopaqueになり、JavaScriptから本文やヘッダーを読めない。

#### セッションCookie

公式ログインはCookieセッションを使用する。クロスサイトCookieにはSameSite等の制約があり、`HttpOnly` CookieはJavaScriptから読めない。PWA化しても公式ドメインのCookieを当アプリが取得・移植することはできない。

#### iframe / 別ウインドウ

公式サイトをiframeまたは別ウインドウで開き、利用者本人にログインしてもらうことは可能な場合がある。しかし、同一オリジン制約により、親PWAからログイン後DOM、表、ダウンロード内容を読むことはできない。公式サイトが `postMessage` 連携を実装した場合のみ別である。

#### Service Worker

Service Workerは当アプリのスコープ内の通信をキャッシュ・制御する仕組みであり、別オリジンであるETC公式サイトのページや認証処理を横取りできない。

### 11.3 技術的には可能だが推奨しない抜け道

#### 自社サーバー中継

当アプリのサーバーへID・パスワードを送り、サーバーが公式サイトへログインすれば、ブラウザーのCORS制約は避けられる。しかし次の問題が新たに生じる。

- 当アプリのサーバーが平文の認証情報を処理する
- 公式セッションCookieを管理する必要がある
- 画面変更、アクセス制限、アカウントロックへの対応が必要
- 公式はユーザー情報を入力する非公式アプリへの注意喚起を公開している
- PWAか通常Webかに関係なく、事業・法務・セキュリティ上の責任を負う

よって「PWAにすれば安全に直接連携できる」わけではない。

#### ブラウザー拡張

公式ページ上で動く拡張機能ならDOMを読む設計は可能だが、これはPWAではない。スマートフォンでの配布性も悪く、公式許可の問題も解決しない。

### 11.4 PWAに最適な採用案

推奨する初期仕様は次のとおり。

1. 「ETC履歴」画面から公式サイトを開く。
2. 利用者が公式サイト上でログインし、公式CSVをダウンロードする。
3. PWAへ戻り、「CSVを読み込む」からファイルを選択する。
4. CSVはブラウザー内で解析し、原則として端末のIndexedDBだけへ保存する。
5. 入口IC、出口IC、利用時刻、割引前、割引、支払額、車両番号を正規化する。
6. 月別カードと明細一覧を表示する。
7. 選択中の月または明細だけ、ICポイントと推定区間を地図へ表示する。

将来、公式APIまたは書面許可が得られた場合は、同じ内部データモデルへ同期アダプターを追加する。CSV版を捨てずに済む。

### 11.5 推奨UX

- 「公式サイトを開く」と「CSVを読み込む」を隣接配置
- 初回だけ3ステップの説明を表示
- ファイル選択後、取込前プレビューで件数・期間・合計額を確認
- 重複、未確定→確定、取消・料金調整を差分表示
- 車両番号とカード番号は初期状態でマスク
- 「この端末だけに保存」「クラウド同期なし」を明示
- 元CSV削除後も、全履歴削除と再インポートを可能にする
- CSV形式変更を「明細0件」と誤認せず、解析エラーとして通知

### 11.6 参考資料

- ETC利用照会サービスは、過去15か月の明細をPDF/CSVで出力できる: https://www.etc-meisai.jp/faq/12.html
- ETC利用照会サービスの非公式アプリへの注意喚起: https://www.etc-meisai.jp/caution/20251219.html
- Same-Origin Policy: https://developer.mozilla.org/en-US/docs/Web/Security/Defenses/Same-origin_policy
- CORS: https://developer.mozilla.org/en-US/docs/Web/HTTP/Guides/CORS
- Fetchの資格情報とCookie: https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API/Using_Fetch
