# 地点・コースの並べ替えと提案のゴール設定

## 自動テスト

`npm run check`

- `draftReorder.test.ts`: 直後へのドロップ、前後への区間移動、座標・ラベルの同期、未指定のゴールを勝手に設定しないこと、不正な移動の拒否。
- `draftInsertion.test.ts`: 提案の終点を経由地／ゴールとして挿入、既存ゴールの維持／経由地への変更、後続地点の保持。

## ブラウザ確認（保存せず下書きで実施）

1. コース作成で箱根ターンパイクと芦ノ湖スカイラインを組み込む。
   - 後から入れたコースが、先のコースの中に隠れず独立した並べ替え行として出る。
2. 「ルートの順番」の下矢印を押す。
   - 隣の行の後ろへ移る。座標とラベルが同じ順で動く。移動が約200msのアニメーションになる。
3. 左側の「⠿」をつかみ、直後の行へドラッグして離す。上方向も試す。
   - コースの全地点がまとまって移り、地点数が変わらない。移動先が緑枠になる。
   - ボトムシート自体がドラッグされない。矢印を連打しても最終的な順番が正しい。
4. 地名・地図指定で追加した個別の地点でも、同様の並べ替えを試す。
5. 「峠道を探す」で候補を取得し「この候補をルートに追加」を押す。
   - 終点の扱いを選ぶ画面が出る。キャンセルでは地点・ゴールが変わらない。
6. 「経由区間として追加」を選ぶ。
   - 既存ゴールがあればその手前に追加。ゴール未指定なら未指定のまま。
7. 「候補を採用する前に戻る」で戻し、同じ候補を「終点をゴールにして追加」する。
   - 元の全地点を残して末尾へ追加。旧ゴールは経由地、新しい区間の終点だけがゴールになる。
8. 提案のプレビュー経由でも同じ選択ができ、キャンセルではプレビューが維持されることを確認。
9. 端末の「視差効果を減らす／アニメーションを減らす」を有効にする。
   - 並べ替えは動作するが、移動アニメーションは省略される。

実施済み: 幅369pxのブラウザで矢印・ドラッグによる区間移動、実際の提案候補の経由地追加／ゴール追加、採用取り消しを確認。iPhone実機のタッチ操作は別途確認が必要。
